# Security and Package-Boundary Cleanup Sprint 1

Date: 2026-05-11
Branch: `chore/course-edition-repo-triage`

## Scope

This sprint is limited to public/course-facing repository safety and package-boundary cleanup. It does not change RedByte product behavior, supported hardware claims, the E0/E1/E2/E3 evidence model, UI workflows, Vivado readiness, or the Runtime Stabilization Sprint 1 Playwright gate.

## Preflight

| Command | Result | Notes |
| --- | --- | --- |
| `git status` | Passed | Worktree started clean except untracked `.redbyte/pi-session-room/`; that local folder was left untouched. |
| `git branch --show-current` | Passed | `chore/course-edition-repo-triage`. |
| `git log --oneline -n 8` | Passed | Latest local commit before this sprint was `4eebcc02 fix: stabilize starter workflow console warnings`. |
| `pnpm -s ide:gate:ece141-starter-verify-export` | Passed | Baseline Logic Gates starter Verify -> Export gate passed before cleanup. |
| `git diff --check` | Passed | Baseline diff whitespace check passed before cleanup. |

## Environment File Review

Commands used:

```powershell
git ls-files -- .env .env.example services/redbyte-intelligence/.env.example
git status --ignored --short
git grep -n -I -E 'API_KEY|SECRET|TOKEN|PASSWORD|PRIVATE|OPENAI|GITHUB|VITE_|DATABASE|SUPABASE|AUTH|KEY=' -- . ':!node_modules' ':!dist' ':!coverage'
```

The secret scan was reviewed without copying secret-like values into this document.

| Path or source | Tracking state before cleanup | Classification | Finding | Action |
| --- | --- | --- | --- | --- |
| `.env` | Tracked | Real local env file; should be untracked | Contains one non-empty `VITE_PUBLIC_DEMO` config value. No secret/token value was identified in this file, but real `.env` files do not belong in the public/course-facing repo. | Removed from git index with `git rm --cached`; local file remains ignored. |
| `.env.example` | Tracked | Safe template | Contains course/demo-style defaults only. No secret values found. | Keep tracked. |
| `services/redbyte-intelligence/.env.example` | Tracked | Safe development-only template | Contains empty Azure/OpenAI-style placeholders and local non-secret defaults. | Keep tracked for now; classify as development-only unless promoted to course tooling. |
| `.github/workflows/*.yml` | Tracked | CI secret references | Uses GitHub Actions secret references and token variables. No hardcoded secret values identified. | Keep tracked. |
| `api/server.mjs` | Tracked | Runtime env usage | Reads `RB_API_TOKEN` from environment. No hardcoded secret value identified. | Keep tracked; course package should document whether this server is in scope. |
| `docs/API_AUTH.md` | Tracked | Documentation with placeholder token examples | Contains example token text for local/classroom auth. No real secret identified, but it is not student quick-start material. | Leave for docs/package-boundary review. |
| Test files with `SECRET` strings | Tracked | Test placeholders | Uses placeholder values for CI/tests only. | Keep tracked. |

Security/privacy conclusion:

- No real secret/token value was found in the inspected tracked files.
- The tracked root `.env` was still a package-boundary problem because it was a real local env file.
- No rotation is required based on this scan. If any deployed credential ever matched a documented/test placeholder, rotate it separately.
- Git history was not rewritten in this sprint.

## Generated Output and Artifact Review

| Path | Current tracking state | Classification | Risk if kept | Risk if removed | Action | Human approval needed? |
| --- | --- | --- | --- | --- | --- | --- |
| `.env` | Tracked before cleanup | Real local env file | Public/course repo may appear to endorse tracking local env files; future secrets could be committed by habit. | Local-only config remains on disk and must be recreated by developers if absent. | Removed from tracking now; `.env.example` remains. | No |
| `coverage/**` | Tracked before cleanup | Generated coverage report | Large stale generated HTML report pollutes source history and can confuse course/package boundaries. | None for source; coverage can be regenerated. | Removed from tracking now; ignored with `coverage/`. | No |
| `apps/playground/dev-server.log` | Tracked before cleanup | Local generated dev-server log | Stale local runtime log in source tree; may accumulate machine-specific output. | None for source; log can be regenerated. | Removed from tracking now; ignored with `**/dev-server.log`. | No |
| `dist/**`, `build/**`, `out/**`, `playwright-report/**`, `test-results/**`, `node_modules/**` | Not tracked in current `git ls-files` scan | Generated build/test/dependency output | Would bloat repo and confuse course package. | None if ignored; regenerated by commands. | Keep ignored/documented. | No |
| `.redbyte/agent/**` | Tracked | Maintainer/agent source and examples | Student-facing confusion if shipped raw. | Could remove useful maintainer workflow docs if deleted prematurely. | Leave for human decision; development-only boundary. | Yes |
| `.redbyte/pi-session-room/` | Untracked local | Local runtime/session material | Should not enter repo. | None. | Left untouched; already untracked. | No |
| `artifacts/**` | Tracked mixed content | Mixed release proof, generated builds, logs, screenshots, zips | Source repo contains bulky/stale/generated material and old OS-era outputs. | Some files may be intentional release proof or fixtures. | Document only; do not broadly remove in this sprint. | Yes |
| `artifacts/classroom-rc-v1/**` and `artifacts/classroom-rc-v1.zip` | Tracked | Historical release bundle/build output | Looks like generated release output and OS-era package material. | Could lose historical course handoff evidence if removed without archive plan. | Leave for archive/removal plan. | Yes |
| `artifacts/**/*.log`, `artifacts/**/*.png`, traces | Tracked mixed content | Logs/screenshots/traces | Machine-specific or one-off debug outputs may remain public. | Some are proof evidence referenced by prior docs. | Leave for artifact audit. | Yes |
| `*.jou`, `*.str`, `*.xpr`, `*.bit`, `*.dcp`, `*.wdb`, `*.rpt`, `.Xil/`, `*.runs/`, `*.cache/` | Not currently tracked in scan | Vivado/local FPGA build output | Would pollute source and create false course evidence if committed accidentally. | None; Vivado outputs are regenerated. | Added ignore patterns. | No |
| `package-lock.json` | Tracked | Stale package-manager artifact | Conflicts with pnpm-only workspace and may lead users toward npm. | Removing without approval may surprise maintainers if any external flow still expects it. | Leave tracked; document removal recommendation. | Yes |

## Package Manager and Lockfile Decision

| Question | Answer |
| --- | --- |
| Is pnpm authoritative? | Yes. `package.json` declares `packageManager: pnpm@10.24.0`, `engines.pnpm >=10.24.0`, scripts use pnpm, `pnpm-lock.yaml` exists, and repo rules say not to run `npm install`. |
| Is `package-lock.json` stale? | Yes. It is tracked alongside the authoritative pnpm lockfile and does not match the pnpm workspace boundary. |
| Is `package-lock.json` actively used? | No active script or repo rule found that requires it. Docs and package scripts point to pnpm. |
| Remove now? | Deferred. Prior course-edition docs classify it as stale but needing approval, so this sprint documents the decision rather than removing it. |
| Should package.json include packageManager? | Already done: `pnpm@10.24.0`. |
| Should docs say pnpm? | Yes. Existing README and AI rules already do; student/professor install docs should repeat this when created. |

## Implemented Cleanup

- Updated `.gitignore` to keep real env files ignored, allow `.env.example` and `.env.template`, ignore `coverage/`, ignore nested `dev-server.log`, and ignore common Vivado/local FPGA build products.
- Removed root `.env` from tracking while leaving the local file on disk.
- Removed tracked `coverage/**` generated coverage report files from the index.
- Removed tracked `apps/playground/dev-server.log` from the index.
- Left mixed or uncertain material documented for later human-approved cleanup.

## Deferred Cleanup

| Area | Reason deferred | Recommended next action |
| --- | --- | --- |
| `artifacts/**` | Mixed proof assets, release bundles, screenshots, logs, generated OS-era packages. | Create an artifact manifest that marks each item as keep, release-asset, fixture, or archive/delete. |
| `.redbyte/agent/**` | Development-only agent material may be useful to maintainers but should not be student-facing. | Decide whether it stays in the public repo, moves private, or remains as clearly marked maintainer-only material. |
| `package-lock.json` | Stale but previous docs requested approval before removal. | Remove in a separate approved package-manager cleanup commit. |
| Old OS-era/playground/Marcus material | Potentially stale but not clearly generated. | Use the archive/removal plan before deletion. |
| `docs/API_AUTH.md` and API/ops auth surface | Could be development/admin-only rather than course-facing. | Classify during professor/admin package-boundary work. |

## Validation

Final validation results for this sprint are recorded in `docs/release/course-edition/08-validation-log.md`.
