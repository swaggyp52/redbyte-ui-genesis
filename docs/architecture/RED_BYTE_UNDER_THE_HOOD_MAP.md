---
doc_status: current
last_validated: 2026-06-14
owner: Connor Angiel
used_by_claude: true
role: RedByte subsystem, state, and proof ownership map
---

# RedByte Under-The-Hood Map

This is the working source map for RedByte V1. It records where product behavior actually lives, which state owner is authoritative, and which tests or gates defend the behavior.

Scope: browser IDE E0 behavior plus the export handoff to Vivado/Basys3 proof tiers. It does not claim fresh Vivado build, board programming, or physical observation proof.

Base audited for this map: `d235823a` on `main`.

## Ownership Rules

- `packages/rb-apps/src/apps/ide/projectRuntime.ts` is the IDE project authority for circuit, scenarios, expected outputs, mapping, project health, export records, import metadata, persistence, and design undo/redo.
- `packages/rb-apps/src/stores/circuitStore.ts` is the editor graph mutation/cache store. It is not the final project authority inside the IDE; `IdeApp.tsx` mirrors runtime state into it and runtime callbacks own committed project changes.
- `packages/rb-logic-view/src/useLogicViewStore.ts` owns canvas camera, selection, interaction, and wiring UI state. It never owns proof, project trust, or export readiness.
- `projectHealth.ts` and `projectWorkflowAuthority.ts` derive trust. Surfaces may display trust; they must not invent it.
- Export files and hashes are generated by Basys3 export services and must agree with current runtime project, verify, and mapping state.
- E0 proof is browser/source/runtime proof. E1/E2/E3 remain Vivado and board proof tiers.

## Subsystem Map

| # | Subsystem | Main files | State owner | Mutation paths | Persistence | Current proof | Known risks / invariants |
|---|---|---|---|---|---|---|---|
| 1 | App boot, routing, mode ownership | `packages/rb-apps/src/apps/IdeApp.tsx`; `workflowStages.ts`; `scripts/gates/ide-route-contract.mjs` | `IdeApp` URL/mode state plus `useProjectRuntime` | mode rail, surface CTAs, URL query mode, import/project load callbacks | URL plus project runtime persist | route/layout gates, classroom gate | Route changes must not bypass project authority or show stale mode state. |
| 2 | Shell, top bar, left rail, inspector, status | `IdeApp.tsx`; `components/IdeWorkbenchShell.tsx`; `components/IdeLeftRail.tsx`; `ide-root.css` | shell props derived from runtime/workflow authority | mode changes, surface policy props, dock collapse/restore | mostly view-local React state | shell chrome/density/layout gates; new `ide:gate:shell-layout-integrity` | One shell authority should dominate; no horizontal overflow; first-viewport focal object stays visible. |
| 3 | Project state and Project surface | `surfaces/ProjectSurface.tsx`; `components/ProjectSurfacePrimitives.tsx`; `projectRuntime.ts`; `projectWorkflowAuthority.ts` | `useProjectRuntime` | starter load, blank project, saved/recovered load, surface CTAs | `rb.ide.project-runtime.v1` and project archive helpers | project overview/health/continue gates; persistence tests | Project copy must not report false mapping/export blockers before a circuit exists. |
| 4 | Circuit graph model | `projectRuntime.ts`; `stores/circuitStore.ts`; `packages/rb-logic-core/src/**` | runtime circuit for IDE project; circuitStore as edit cache | add/remove/update nodes and wires, starter load, import apply, undo/redo | runtime persist; export/import `.rbproj` | circuitStore canonical/fingerprint tests; design gates | Runtime and editor cache must not diverge after edits, navigation, reload, or undo/redo. |
| 5 | Design canvas rendering | `surfaces/DesignSurface.tsx`; `packages/rb-logic-view/src/LogicCanvas.tsx`; `ide-root.css` | `useLogicViewStore` camera plus runtime/circuitStore graph | graph changes, viewport fit, zoom, pan, layout, resize | camera is view state, not project proof | design canvas zoom integrity; design workbench contract; new design workbench integrity | Rendered SVG must never receive `NaN`/`Infinity`; starter graph must stay visible at classroom sizes. |
| 6 | Design interactions | `DesignSurface.tsx`; `LogicCanvas.tsx`; `circuitStore.ts`; `useLogicViewStore.ts` | circuitStore mutation path mirrored into runtime; view store for transient interaction | place, move, wire, select, delete, cancel, palette, fit/center/zoom | committed graph through runtime; transient interaction is not persisted as proof | wire interaction, placement, multiselect, inspector, new design workbench integrity | Delete/undo must restore exact graph intent; wiring cancellation must leave no phantom proof. |
| 7 | Undo/redo history | `projectRuntime.ts`; `circuitStore.ts`; `projectRuntime.history-authority.test.tsx` | runtime `designPast` / `designFuture` for IDE | design mutation commit, undo, redo, starter load, import apply | runtime persisted project fields | runtime history tests; new design workbench integrity delete/undo step | Runtime history wins over editor-cache history for user-visible IDE project truth. |
| 8 | Circuit health and diagnostics | `projectHealth.ts`; `DesignSurface.tsx`; `projectWorkflowAuthority.ts`; `workflowStages.ts` | derived from runtime circuit and compiler/simulation checks | circuit edits, verify runs, mapping edits, export builds | derived and/or runtime health core | diagnostics jump, project health live, synth subset gates | Diagnostics must invalidate stale proof instead of coexisting with trusted export labels. |
| 9 | Verify scenario model | `projectRuntime.ts`; `surfaces/VerifySurface.tsx`; `components/StimulusCanvas.tsx`; `fpga/boards/basys3/vectorRunner.ts` | runtime scenarios/project vectors | row edit, observe run, compare run, starter load | runtime persisted vectors/scenarios | verify authority/runtime tests; verify gates | Scenario edits must stale previous compare proof until rerun. |
| 10 | Expected-output grid | `StimulusCanvas.tsx`; `VerifySurface.tsx`; `projectRuntime.ts` | runtime expected values in project vectors/scenarios | save observed as expected, edit expected cell, repair edit | runtime persisted vectors | `ide:gate:verify-fail-edit-repair`; expected-output tests | Edited expected output must flip PASS to stale, then allow an intentional FAIL and repair rerun. |
| 11 | Observe-only path | `VerifySurface.tsx`; `projectRuntime.ts`; `vectorRunner.ts` | runtime verify last run plus observed output rows | observe run, save expected outputs | runtime last run/history | verify contract/reality/summary gates | Observe is evidence collection, not a passing proof unless Compare validates expected outputs. |
| 12 | Compare-check path | `VerifySurface.tsx`; `projectRuntime.ts`; `projectHealth.ts`; `projectWorkflowAuthority.ts` | runtime `verifyLastRun`, `verifyRunHistory`, `projectHealthCore.lastVerify` | compare run, expected edits, circuit edits, mapping edits | runtime health core/history | verify fail-edit-repair, verify workbench, projectWorkflowAuthority tests | Compare PASS must be current for the same circuit/vector/mapping state before Export can be trusted. |
| 13 | Waveform rendering | `surfaces/verify/WaveformInstrument.tsx`; `VerifySurface.tsx`; `VerifyWaveformPlaceholder.tsx` | derived from verify run timeline | observe/compare run, tick selection, signal filters | not proof authority; derived display | verify workbench/summary gates; waveform tests | Waveform may explain proof, but cannot replace pass/fail and expected/observed authority. |
| 14 | Failure diff and repair path | `VerifySurface.tsx`; `StimulusCanvas.tsx`; `projectWorkflowAuthority.ts` | compare result plus expected-output edit state | run Compare FAIL, open failing check, edit expected output, rerun | runtime scenario/health state | `ide:gate:verify-fail-edit-repair` | First mismatch must be findable and repair must not silently preserve stale PASS. |
| 15 | Project health and trust | `projectHealth.ts`; `projectWorkflowAuthority.ts`; `workflowStages.ts` | `projectHealthCore` plus derived workflow authority | circuit/vector/mapping/export mutations, verify/export runs | runtime persisted health core | projectHealth tests; workflowAuthority tests; student loop gates | CLEAN, STALE, FAIL, Draft, and Trusted states must be derived from matching hashes. |
| 16 | Map Pins / hardware mapping | `projectRuntime.ts`; `hardwareMappingV2EditorModel.ts`; `hardwareMappingBridge.ts`; `HardwareSurface.tsx` | runtime `hardwareMappingV2`; `projectIoRows` is a projection | map/unmap pin, auto rows, starter import, board clock auto | runtime persisted mapping fields | mapping authority tests; hardware gates; from-scratch workflow | V2 mapping and flat row projection must stay synchronized. |
| 17 | Basys3 board model | `fpga/boards/basys3/**`; `components/boards/Basys3Board*.tsx`; `components/HardwareBoard2D.tsx` | board profile and Basys3 pin/resource contracts | map row to resource, board click, export generation | board profile is source data; mapping persisted in runtime | Basys3 tests; hardware checklist/visual gates | Board-ready language must stay E0 unless Vivado/board proof exists. |
| 18 | Export trust, Draft vs Trusted | `ExportSurface.tsx`; `surfaces/export/ExportSurfacePrimitives.tsx`; `projectWorkflowAuthority.ts`; `projectHealth.ts` | workflow authority plus runtime export record | build bundle, download draft/trusted package, stale after edits | runtime `lastExport`; package file records | export ready/blockers/download/e2e gates; workflow tests | Export must not imply trusted Vivado handoff without current Compare PASS, mapping, and export for same project state. |
| 19 | VHDL/XDC/testbench/Tcl generation | `fpga/boards/basys3/basys3ExportService.ts`; `testbenchGenerator.ts`; `vivadoImportTcl.ts`; `packages/rb-fpga-toolchain/src/**` | generator inputs from runtime circuit/scenarios/mapping | export build | ZIP contents and hashes | golden export gates; vivado artifact tests; export e2e | Generation changes require source-explained hash/golden updates, not screenshot-only proof. |
| 20 | Import and project recovery | `surfaces/ImportSurface.tsx`; `zipImport.ts`; import fixtures/tests; `projectPersistence.ts` | Import surface local candidate until apply; runtime after apply | upload ZIP, parse, review, apply, route to Design | runtime after apply; archive helpers | zip import gates; import render/roundtrip tests | Normal loaded-project access is currently ambiguous; Import must not replace project before review/apply. |
| 21 | Examples, starters, no-solution | `examples/**`; `examplesCatalog.ts`; `starterKits/**`; no-solution tests | catalog/source JSON plus starter load into runtime | starter select/load | project runtime after load | examples contract; no-solution CI; lab starter tests | Starters must be general enough for V1 and must not leak solution answers where blocked. |
| 22 | Lab profile data seam | `docs/product/RED_BYTE_LAB_PROFILE_MODEL.md`; `packages/rb-apps/src/apps/ide/labProfiles/**`; starter/catalog modules | profile metadata only; not runtime circuit authority | profile lookup and validation; future profile-backed lab load | future course data, not active project proof | `lab:profile-contract` | Course metadata must not own Basys3, Verify, mapping, export, proof-tier, or project-runtime semantics. |
| 23 | Persistence, local storage, archive | `projectRuntime.ts`; `apps/ide/projectPersistence.ts`; `services/projectPersistence.ts` | runtime persisted subset and archive helpers | autosave, load, reset, import/export roundtrip | `rb.ide.project-runtime.v1`, `.rbproj`, ZIP | persistence contract/tests; rbproject roundtrip | Persisted proof must invalidate when source state changes, not resurrect old trust. |
| 24 | Browser gates and Playwright harness | `scripts/gates/_gateHarness.mjs`; `scripts/gates/*.mjs`; `scripts/classroom-gate.mjs` | test harness launches preview/dev app and browser state | gate scripts, screenshots, seeded localStorage | generated local artifacts only | classroom gate; focused gates; new integrity gates | Gates must assert current UI truth, not stale selectors or retired flows. |
| 25 | Vitest and pure logic tests | `packages/**/__tests__/**`; `vitest.config.*`; `scripts/verify-gates-classroom.mjs` | source modules under test | unit/integration test updates | no app persistence | determinism/parity suite; focused runtime tests | Use Vitest for state/logic contracts and browser gates for rendered workflows. |
| 26 | GitHub CI, Classroom, Nightly | `.github/workflows/**`; `scripts/classroom-gate.mjs`; `scripts/verify-gates-classroom.mjs`; `docs/development/RED_BYTE_GITHUB_OPERATIONS.md` | GitHub Actions plus branch protection | push, workflow dispatch, schedule | GitHub run history | Classroom Truth Gates, Deploy to Cloudflare Pages, Nightly where relevant | Required checks must be watched after push; skipped optional jobs are not failures. |
| 27 | Deploy and Cloudflare verification | Cloudflare Pages workflow/checks; `docs/development/RED_BYTE_GITHUB_OPERATIONS.md` | deploy pipeline, not local app | push to `main` | hosted deployment result | Deploy to Cloudflare Pages check | "Pushed to GitHub" is source delivered. It is not "live for students" unless deploy check confirms that commit. |

## Cross-Cutting Invariants

- Project state changes must stale Verify and Export proof unless the derived hashes still match.
- Browser proof must show the visible build hash matches the repo commit being claimed.
- Local `.redbyte/product-immersion/**` artifacts are useful evidence but not portable proof in a clean clone.
- Generated HDL/XDC/testbench/Tcl/ZIP bytes are not UI artifacts. They need generator tests and golden/hash proof when changed.
- Manual/product docs are not allowed to outrank source, focused tests, or current runtime evidence.

## Current Audit Findings

The 2026-06-13 normal-use audit found the main student spine coherent on current build `Buildd235823`, with no console or page errors across the exercised Project, Design, Verify, Hardware, and Export paths. Later slices closed the two product-control findings from that audit:

- Export artifact preview and handoff station visibility are covered by `ide:gate:export-trust-integrity` and `ide:gate:export-handoff-station`.
- Import utility access and recovery language are covered by `ide:gate:import-recovery-contract`.

The next open product-control gap is public student/instructor quickstart documentation, not a runtime authority change.

## Attribution

Connor Angiel
