# RedByte v1 — Canonical Truth Document

*Produced by finish engineering audit, 2026-03-08. Authoritative.*

---

## What the canonical student app IS

The one canonical student environment is the **IDE Lab Shell** at `packages/rb-apps/src/apps/IdeApp.tsx`
and its six surfaces (Project, Design, Verify, Hardware, Export, Import), deployed at `https://redbyteapps.dev/os/`.

---

## What is deployed

| Property | Value |
|----------|-------|
| URL | `https://redbyteapps.dev/os/` |
| Root (`/`) | Minimal redirect stub → `/os/` (generated by `scripts/merge-dist.mjs`) |
| Artifact | `dist/` — root stub + `/os/` playground IDE |
| Build command | `pnpm build:unified` (calls `scripts/unified-build.mjs`) |
| Deploy workflow | `.github/workflows/deploy-cloudflare.yml` |
| Node version | `20.19.0` (per CI_CONTRACT.md) |
| pnpm version | `10.24.0` (per CI_CONTRACT.md) |
| Version proof | `https://redbyteapps.dev/os/version.json` → `{ sha, builtAt }` |

---

## What is legacy / dead code (quarantined to archive/)

- OS-era app components: `OscilloscopeView.tsx`, `OscilloscopePanel.jsx`, `SplitViewLayout.tsx`, `BoardIOPanel.js`, `TestVectorPanel.js`, `TruthTableAnalyzer.js`
- 31 OS-era test files (FilesApp, TerminalApp, UserManualApp, LabWorkspace, evidenceCapsule, etc.)
- Backup files: `OscilloscopeView.tsx.backup`, `SplitViewLayout.tsx.bak`
- All quarantined to: `archive/dead-legacy-components/`

## What is experimental / standalone (not in student workflow)

- `rb-fpga-bridge` — hardware bridge agent, standalone nightly service
- `ops` / `ops-server` — backend ops service, standalone

## What packages do NOT exist (referenced in aliases only)

- `@redbyte/rb-windowing` — OS window manager; minimal stub at `packages/rb-windowing/src/index.ts` for vitest alias only
- `@redbyte/rb-shell` — OS app shell; does not exist
- `@redbyte/rb-logic-3d` — 3D visualizer; does not exist
- `@redbyte/rb-analog-sim` — analog sim; does not exist

---

## Commands that define "release done"

All of the following must exit 0:

```bash
# 1. All unit/gate tests
pnpm --filter @redbyte/rb-apps test
# Expected: 90 passed, 0 failed

# 2. All 27 classroom quality gates
pnpm verify:gates
# Expected: EXIT 0

# 3. Combined release candidate check
pnpm rc:check
# Expected: EXIT 0

# 4. Unified build produces valid deploy artifact
pnpm build:unified
# Expected: EXIT 0; dist/os/index.html has REDBYTE_OS_IDE marker; dist/index.html redirects to /os/

# 5. Submission bundle integrity (run with a real exported ZIP)
pnpm v1:verify -- <exported-bundle.zip>
```

### Additional deployment proof
After merging to main:
```
https://redbyteapps.dev/os/version.json → { "sha": "<commit-sha>", "builtAt": "..." }
```
The `sha` must match the triggering commit.

---

## Determinism invariants (must remain true)

- Same circuit + same vectors → same `reportHash` every run
- Same circuit + same ioMapping → same ZIP bundle bytes (deterministic date `2000-01-01T00:00:00Z`)
- Gates: `rc:d0:project-determinism-gate`, `rc:d1:verilog-determinism-gate`, `rc:d2:basys3-bundle-gate`, `rbx:evidence-determinism-gate`
