# Contributing to RedByte UI Genesis

- Use pnpm for all workspace operations.
- Keep TypeScript `strict` enabled in every package.
- Run `pnpm lint`, `pnpm typecheck`, and `pnpm test` before submitting changes.
- Prefer @rb/* path aliases rather than long relative imports.
