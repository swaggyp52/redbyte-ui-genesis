# Cloudflare Pages Build Configuration
# Build command: pnpm build:unified
# Build output directory: dist
# Root directory: /

# Cloudflare Pages installs dependencies automatically; do not prefix the build command with pnpm install.
# Unified build emits dist/ with a root redirect stub and the IDE at /os/.
