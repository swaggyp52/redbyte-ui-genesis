#:schema node_modules/wrangler/config-schema.json
# Cloudflare Pages configuration for unified RedByte deploy
# 
# NOTE: Build command is controlled via GitHub Actions workflow.
# Pages will auto-detect the build from the workflow deployment,
# or you can set it in the Cloudflare Pages dashboard.

name = "redbyte-ui-genesis"
pages_build_output_dir = "dist"
compatibility_date = "2024-01-01"
