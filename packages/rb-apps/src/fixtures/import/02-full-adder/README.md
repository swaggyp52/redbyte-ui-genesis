# Full Adder Fixture (Basys3)

One-bit full adder design with three switch inputs (SW0, SW1, SW2) and two LED outputs (LD0, LD1). Demonstrates multi-input/multi-output structural HDL with Basys3 pin mapping. Used for roundtrip import→export validation.
