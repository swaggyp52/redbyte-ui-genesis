# AND Gate Fixture (Basys3)

This is a minimal AND gate design demonstrating structural HDL import with Basys3 pin constraints. The circuit takes two switch inputs (SW0, SW1) and drives one LED output (LD0). Used to validate import→export roundtrip functionality.
