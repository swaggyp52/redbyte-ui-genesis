# 4-bit Counter Fixture (Basys3, Clocked)

A realistic sequential design demonstrating clock/reset handling, bus-indexed outputs (`count[3:0]`), and varied XDC formatting (no-space braces, IOSTANDARD directives, create_clock constraint). This fixture validates that the parser handles real Vivado output without crashing on format variants or unsupported directives.
