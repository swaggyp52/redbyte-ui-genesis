import React, { useCallback, useEffect, useMemo, useRef, useState } from 'react';
import { ProblemsPanel } from '../components/ProblemsPanel';
import { selectProblemCount, useEngineeringProblems } from '../engineeringProblems';
import { useEngineeringSelection } from '../engineeringSelection';
import type { WorkbenchDocument } from '../workbenchDocuments';
import { HandoffOverviewDocument } from './export/HandoffOverviewDocument';
import type { IdeExampleDefinition } from '../examplesCatalog';
import type { RBProject } from '../../../export/projectFormat';
import { buildDeterministicZip, sha256Hex } from '../../../export/deterministicZip';
import {
  deriveProjectVerifyState,
  type ProjectHealthExportResult,
  type ProjectHealthExportSourceState,
  type ProjectHealthVerifyResult,
} from '../projectHealth';
import { createIdeDiagnostic, type IdeDiagnostic } from '../diagnostics';
import {
  deriveHardwareExportFailureTruth,
  deriveProjectWorkflowAuthority,
  type ProjectWorkflowAuthority,
} from '../projectWorkflowAuthority';
import {
  buildVivadoProjectFolderZip,
  deriveVivadoProjectSlug,
  resolveVivadoPart,
} from '../../../fpga/vivado/vivadoProjectFolder';
import { deriveVerifySchedule, type VerifyScheduleContract } from '../../../fpga/boards/basys3/verifySchedule';
import { resolveBasys3BoardAlias, resolveBasys3PackagePin } from '../../../fpga/boards/basys3/basys3Pins';
import type { RuntimeVerifyRun } from '../projectRuntime';
import { resolveActiveScheduleContract } from '../clockAuthority';
import {
  buildExportViewModel,
  buildLiveIoNodeLabelIndex,
  resolveMappingPortName,
  type ExportDiagnosticSeverity,
  type ExportDiagnosticView,
  type ExportArtifactView,
  type ExportPinTableRow,
  type ExportPinStatus,
} from '../viewmodels/buildExportViewModel';
import type { VerifyScenario } from '../verifyScenario';
import { resolveIoMappingFromProjectFields } from '@redbyte/rb-utils';
import { deriveTimingGuidance, type TimingGuidance } from '../timingGuidance';
import type {
  FullAdderLabExportSummary,
  GuidedLabTaskDefinition,
} from '../labTaskDefinition';
import {
  buildArtifactAgreementRows,
  derivePackageHandoffSummary,
  formatExportDiagnosticOwner,
  formatTimingPlain,
  worstAgreementTone,
} from '../exportPackageHandoffModel';
import { IdeSurfaceLayout } from '../components/IdeSurfaceLayout';
import {
  IdeButton,
  IdeBlockedState,
  IdeCallout,
  IdeInspectorSection,
  IdePanel,
  IdeSpinner,
  IdeStatusPill,
} from '../components/IdePrimitives';
import type { IdeChromeContract } from '../chromeContract';
import { PROFESSIONAL_CLASSROOM_COPY } from '../productUiStandards';
import {
  buildProjectExportPackageSourceHash,
  deriveExportVerificationTrust,
  deriveExportTrustAxes,
  formatExportDerivedState,
  isExportDownloadReceiptCurrent,
} from '../exportTrustState';
import {
  ExportVivadoInstructions,
  ExportAdvancedDetails,
  type ExportVerifyProvenance,
  type ExportDebugCopyState,
} from './export/ExportSurfacePrimitives';
import './export-handoff-workspace-v3.css';
import './export/package-instrument.css';
import {
  deriveBehavioralEvidenceTier,
  deriveBehavioralEvidenceTierFromResult,
  formatBehavioralEvidenceTier,
} from '../simulationEvidence';
import { ExportHistoryPanel } from '../components/ExportHistoryPanel';

export const CHROME_CONTRACT = {
  surfaceId: 'export',
  topStripSlots: ['command-bar'],
  leftDockPolicy: 'hidden',
  rightDockPolicy: 'always',
  exitPaths: [],
} satisfies IdeChromeContract;

export interface ExportSurfaceProps {
  project: RBProject;
  verifyResult?: ProjectHealthVerifyResult;
  verifyLastRun?: RuntimeVerifyRun;
  /** Browser-local evidence for the last package download. */
  lastExport?: ProjectHealthExportResult;
  /** Bounded ledger of prior generation/download events (newest last). */
  exportHistory?: ProjectHealthExportResult[];
  /** Structural Design authority. Prior Compare evidence is inconclusive while present. */
  designBlockingIssue?: {
    title: string;
    message: string;
    hint?: string;
  };
  designReady?: boolean;
  workflowAuthority?: ProjectWorkflowAuthority;
  /** The currently active Verify scenario - used as the authoritative testbench vector source. */
  activeScenario?: VerifyScenario;
  dirtySinceVerify?: boolean;
  determinismHash: string;
  /** The workbench document this surface renders: the package files, or the handoff overview. */
  activeDocument?: WorkbenchDocument | null;
  onOpenDocument?: (doc: WorkbenchDocument) => void;
  /** True when the project vectors were auto-generated (not authored by the student).
   *  Softens export gating for starter-scenario fail cases. */
  vectorsAreAutoGenerated?: boolean;
  onExportBundle?: (artifacts: ExportArtifactView[]) => void;
  onExportResult?: (result: ProjectHealthExportResult) => void;
  onDiagnosticAction?: (diagnostic: IdeDiagnostic) => void;
  onOpenVerify?: () => void;
  example?: IdeExampleDefinition | null;
  onGoToHardware?: () => void;
  onGoToProject?: () => void;
  onGoToDesign?: () => void;
  onUpdateMappingPin?: (rowId: string, pin: string) => void;
  timingGuidance?: TimingGuidance;
  guidedLabTask?: GuidedLabTaskDefinition | null;
  guidedLabExportSummary?: FullAdderLabExportSummary | null;
}

// ─── Phase 32: Rebuild Pipeline Types ──────────────────────────────────────
type RebuildStepId =
  | 'validate'
  | 'mapping'
  | 'clock'
  | 'bundle'
  | 'manifest'
  | 'zip';

type RebuildStepState = 'idle' | 'running' | 'done' | 'warn' | 'error' | 'skipped';
type ExportLayoutMode = 'wide' | 'standard' | 'compact';
type ExportArtifactGroupId = 'project' | 'source' | 'constraints' | 'simulation' | 'readme';

interface ExportArtifactGroup {
  id: ExportArtifactGroupId;
  label: string;
  description: string;
  artifacts: ExportArtifactView[];
}

interface RebuildStep {
  id: RebuildStepId;
  label: string;
  state: RebuildStepState;
  detail?: string;
}

const STEP_ORDER: Array<{ id: RebuildStepId; label: string }> = [
  { id: 'validate', label: 'Validate inputs' },
  { id: 'mapping',  label: 'Validate I/O mapping' },
  { id: 'clock',    label: 'Validate clock domain' },
  { id: 'bundle',   label: 'Build VHDL + constraints' },
  { id: 'manifest', label: 'Generate README' },
  { id: 'zip',      label: 'Package Vivado Project' },
];

const FIRST_VIEWPORT_ARTIFACT_PATHS = [
  'README.txt',
  'top.vhd',
  'top.xdc',
  'testbench.vhd',
  'vivado_import.tcl',
] as const;

const VIVADO_SUBMISSION_GUIDANCE: ReadonlyArray<{
  role: ExportArtifactGroupId;
  label: string;
  guidance: string;
}> = [
  {
    role: 'project',
    label: 'Project',
    guidance: 'Use the generated Vivado project or import Tcl when the submission asks for a reproducible project setup.',
  },
  {
    role: 'source',
    label: 'Source',
    guidance: 'Submit top.vhd and any companion design sources when HDL source files are requested.',
  },
  {
    role: 'constraints',
    label: 'Constraints',
    guidance: 'Submit top.xdc when the grader needs board pin and timing constraints.',
  },
  {
    role: 'simulation',
    label: 'Simulation',
    guidance: 'Submit testbench.vhd only when simulation sources or evidence are requested.',
  },
  {
    role: 'readme',
    label: 'README',
    guidance: 'Include README.txt or BRINGUP.md when setup, provenance, or board-use notes are part of the handoff.',
  },
];

function makeSteps(): RebuildStep[] {
  return STEP_ORDER.map((s) => ({ id: s.id, label: s.label, state: 'idle' as const }));
}

/** Clock gate row detail: same timing classification as Simulate / Board & Constraints when hashes align. */
function formatExportClockGateDetail(
  contract: VerifyScheduleContract | undefined,
  baseDetail: string
): string {
  const mode = contract?.timingMode;
  if (mode === 'manual_event_driven_lab') return `${baseDetail} · Manual-event lab`;
  if (mode === 'synchronous_board_clock') return `${baseDetail} · Board clock`;
  if (mode === 'combinational') return `${baseDetail} · Combinational`;
  return baseDetail;
}

function formatExportClockDeterministicTooltip(
  contract: VerifyScheduleContract | undefined,
  baseTooltip: string
): string {
  const mode = contract?.timingMode;
  if (mode === 'manual_event_driven_lab') {
    return `${baseTooltip} RedByte treats this as manual-event lab timing: interactive inputs drive state; export relaxes those paths and does not require a board oscillator net.`;
  }
  if (mode === 'synchronous_board_clock') {
    return `${baseTooltip} Map the primary clock to a clock-capable pin so synthesis timing matches the board.`;
  }
  return baseTooltip;
}

/** Yields to the event loop so React can flush intermediate step state. */
function tick(ms = 40): Promise<void> {
  return new Promise((resolve) => window.setTimeout(resolve, ms));
}

export const ExportSurface: React.FC<ExportSurfaceProps> = ({
  project,
  verifyResult: persistedVerifyResult,
  verifyLastRun: persistedVerifyLastRun,
  lastExport,
  exportHistory = [],
  designBlockingIssue,
  designReady = true,
  workflowAuthority,
  activeScenario,
  dirtySinceVerify = false,
  determinismHash,
  activeDocument = null,
  onOpenDocument,
  vectorsAreAutoGenerated = false,
  onExportBundle,
  onExportResult,
  onDiagnosticAction,
  onOpenVerify,
  example,
  onGoToHardware,
  onGoToProject,
  onGoToDesign,
  onUpdateMappingPin,
  timingGuidance,
  guidedLabTask,
  guidedLabExportSummary,
}) => {
  const verifyBlockedByDesign = Boolean(
    designBlockingIssue || workflowAuthority?.verifyBlockedByDesign
  );
  const verifyResult = verifyBlockedByDesign ? undefined : persistedVerifyResult;
  const verifyLastRun = verifyBlockedByDesign ? undefined : persistedVerifyLastRun;
  const surfaceRef = useRef<HTMLElement | null>(null);
  const hasExternalMappingAuthority = Boolean(onUpdateMappingPin);
  const baseViewModel = useMemo(
    () => buildExportViewModel(project, verifyLastRun, activeScenario),
    [project, verifyLastRun, activeScenario]
  );
  const [pinDrafts, setPinDrafts] = useState<Record<string, string>>(() =>
    createPinOverrideMap(baseViewModel.pinTable)
  );
  const localPreviewProject = useMemo(
    () => applyPinOverridesToProject(project, pinDrafts),
    [pinDrafts, project]
  );
  const viewModel = useMemo(
    () =>
      hasExternalMappingAuthority
        ? baseViewModel
        : buildExportViewModel(localPreviewProject, verifyLastRun, activeScenario),
    [activeScenario, baseViewModel, hasExternalMappingAuthority, localPreviewProject, verifyLastRun]
  );
  const topModule = useMemo(() => resolveTopEntity(project), [project]);
  const projectSlug = useMemo(
    () => deriveVivadoProjectSlug((project.meta?.projectId ?? project.name ?? '').trim()),
    [project.meta?.projectId, project.name]
  );
  const vivadoPart = resolveVivadoPart(project.fpga?.part);
  const packageSourceHashes = useMemo(
    () => ({
      project: buildProjectExportPackageSourceHash(project, viewModel.artifacts, 'project'),
      kit: buildProjectExportPackageSourceHash(project, viewModel.artifacts, 'kit'),
    }),
    [project, viewModel.artifacts]
  );
  const liveScheduleContract = useMemo(() => {
    const ioForSchedule =
      resolveIoMappingFromProjectFields({
        ioMapping: project.ioMapping,
        hardwareMappingV2: project.hardwareMappingV2,
      }) ?? project.ioMapping;
    return deriveVerifySchedule(project.circuit, ioForSchedule, project.hdl);
  }, [project.circuit, project.hardwareMappingV2, project.hdl, project.ioMapping]);
  const activeScheduleContract = useMemo(
    () =>
      resolveActiveScheduleContract({
        deterministicHash: determinismHash,
        liveScheduleContract,
        lastRun: verifyLastRun,
      }),
    [determinismHash, liveScheduleContract, verifyLastRun]
  );
  const effectiveTimingGuidance = useMemo(
    () => timingGuidance ?? deriveTimingGuidance(activeScheduleContract),
    [timingGuidance, activeScheduleContract]
  );
  const timingPlain = useMemo(
    () => formatTimingPlain(activeScheduleContract?.timingMode, effectiveTimingGuidance),
    [activeScheduleContract?.timingMode, effectiveTimingGuidance]
  );
  const evidenceDiagnostics = useMemo(
    () =>
      buildEvidenceDiagnostics(
        verifyResult,
        dirtySinceVerify,
        vectorsAreAutoGenerated,
        verifyLastRun?.qualification
      ),
    [dirtySinceVerify, vectorsAreAutoGenerated, verifyLastRun?.qualification, verifyResult]
  );
  const problemsLedgerCount = useEngineeringProblems(selectProblemCount);
  const packageProblemCount = useEngineeringProblems(
    (state) => state.problems.filter((problem) => problem.category === 'package' || problem.category === 'constraint' || problem.category === 'board').length
  );
  const diagnosticsList = useMemo(
    () => [...viewModel.errors, ...evidenceDiagnostics, ...viewModel.warnings],
    [evidenceDiagnostics, viewModel.errors, viewModel.warnings]
  );
  const [downloadError, setDownloadError] = useState<string>('');
  const [sessionDownloadEvidence, setSessionDownloadEvidence] = useState<ProjectHealthExportResult>();
  const [packageValidation, setPackageValidation] = useState<{
    status: 'ready' | 'blocked';
    message: string;
  } | null>(null);
  const [copiedTarget, setCopiedTarget] = useState<string | null>(null);
  const [copyError, setCopyError] = useState(false);
  const [technicalEvidenceOpen, setTechnicalEvidenceOpen] = useState(false);
  const [highlightedPort, setHighlightedPort] = useState<string | null>(null);
  const [selectedArtifactPath, setSelectedArtifactPath] = useState<string>(() => {
    const topVhd = baseViewModel.artifacts.find((artifact) => artifact.path.toLowerCase() === 'top.vhd');
    const readme = baseViewModel.artifacts.find((artifact) => artifact.path.toLowerCase() === 'readme.txt');
    return topVhd?.path ?? readme?.path ?? baseViewModel.artifacts[0]?.path ?? '';
  });
  const [openFixPathId, setOpenFixPathId] = useState<string | null>(null);

  // ── Engineering-object continuity ──────────────────────────────────────
  const globalSelected = useEngineeringSelection((state) => state.selected);
  const globalOrigin = useEngineeringSelection((state) => state.origin);
  const publishSelection = useEngineeringSelection((state) => state.select);
  useEffect(() => {
    if (!selectedArtifactPath) return;
    const next = { kind: 'artifact' as const, artifactId: selectedArtifactPath };
    if (globalSelected && JSON.stringify(globalSelected) === JSON.stringify(next)) return;
    publishSelection(next, 'package-artifact');
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [selectedArtifactPath]);
  useEffect(() => {
    if (!globalSelected || globalOrigin === 'package-artifact' || globalSelected.kind !== 'artifact') return;
    if (globalSelected.artifactId !== selectedArtifactPath && baseViewModel.artifacts.some((artifact) => artifact.path === globalSelected.artifactId)) {
      setSelectedArtifactPath(globalSelected.artifactId);
    }
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [globalSelected, globalOrigin]);
  // Phase 32: pipeline rebuild state
  const [rebuildSteps, setRebuildSteps] = useState<RebuildStep[]>(() => makeSteps());
  const [isRebuilding, setIsRebuilding] = useState(false);
  const [layoutMode, setLayoutMode] = useState<ExportLayoutMode>(() => resolveExportLayoutMode());
  // Phase 2: track which port keys have invalid (non-Basys3) pin values
  const [invalidPins, setInvalidPins] = useState<Set<string>>(new Set());
  const rowRefs = useRef<Record<string, HTMLTableRowElement | null>>({});
  const mapSectionRef = useRef<HTMLElement>(null);
  const artifactPreviewSectionRef = useRef<HTMLElement>(null);
  const highlightResetTimer = useRef<number | null>(null);
  const copyResetTimer = useRef<number | null>(null);
  const editablePortKeys = useMemo(() => collectMappedProjectPortKeys(project), [project]);

  useEffect(() => {
    const nextOverrides = createPinOverrideMap(baseViewModel.pinTable);
    if (hasExternalMappingAuthority) {
      // External authority mode reads directly from project mapping state.
      setPinDrafts({});
      setInvalidPins(new Set());
    } else {
      // Standalone mode keeps local drafts so preview updates without parent wiring.
      setPinDrafts(nextOverrides);
      setInvalidPins(buildInvalidPinSet(nextOverrides, editablePortKeys));
    }
  }, [baseViewModel.pinTable, editablePortKeys, hasExternalMappingAuthority]);

  useEffect(() => {
    if (viewModel.artifacts.length === 0) {
      setSelectedArtifactPath('');
      return;
    }
    const exists = viewModel.artifacts.some((artifact) => artifact.path === selectedArtifactPath);
    if (!exists) {
      const topVhd = viewModel.artifacts.find((artifact) => artifact.path.toLowerCase() === 'top.vhd');
      const readme = viewModel.artifacts.find((artifact) => artifact.path.toLowerCase() === 'readme.txt');
      setSelectedArtifactPath(topVhd?.path ?? readme?.path ?? viewModel.artifacts[0].path);
    }
  }, [viewModel.artifacts, selectedArtifactPath]);

  useEffect(() => {
    if (typeof window === 'undefined') return;
    const surface = surfaceRef.current;
    if (!surface || typeof ResizeObserver === 'undefined') {
      setLayoutMode(resolveExportLayoutMode());
      return;
    }
    const observer = new ResizeObserver((entries) => {
      const nextWidth = entries[0]?.contentRect.width ?? surface.clientWidth;
      setLayoutMode(resolveExportLayoutMode(nextWidth));
    });
    observer.observe(surface);
    setLayoutMode(resolveExportLayoutMode(surface.clientWidth));
    return () => observer.disconnect();
  }, []);

  useEffect(() => {
    return () => {
      if (highlightResetTimer.current !== null && typeof window !== 'undefined') {
        window.clearTimeout(highlightResetTimer.current);
      }
      if (copyResetTimer.current !== null && typeof window !== 'undefined') {
        window.clearTimeout(copyResetTimer.current);
      }
    };
  }, []);

  const mappingIndex = useMemo(() => {
    const index = new Map<string, (typeof viewModel.pinTable)[number]>();
    for (const row of viewModel.pinTable) {
      index.set(toPortKey(row.port), row);
    }
    return index;
  }, [viewModel.pinTable]);
  const authoritativePinsByPortKey = useMemo(() => {
    const pins: Record<string, string> = {};
    for (const row of viewModel.pinTable) {
      pins[toPortKey(row.port)] = row.pin ?? '';
    }
    return pins;
  }, [viewModel.pinTable]);
  const [recentlyReconciledPins, setRecentlyReconciledPins] = useState<Set<string>>(new Set());
  const previousAuthoritativePins = useRef<Record<string, string> | null>(null);

  useEffect(() => {
    const previous = previousAuthoritativePins.current;
    previousAuthoritativePins.current = authoritativePinsByPortKey;
    if (!hasExternalMappingAuthority) {
      setRecentlyReconciledPins(new Set());
      return;
    }
    if (!previous) return;

    const nextChangedPins = new Set<string>();
    const keys = new Set<string>([
      ...Object.keys(previous),
      ...Object.keys(authoritativePinsByPortKey),
    ]);
    for (const key of keys) {
      if (!editablePortKeys.has(key)) continue;
      const before = (previous[key] ?? '').trim();
      const after = (authoritativePinsByPortKey[key] ?? '').trim();
      if (before !== after) {
        nextChangedPins.add(key);
      }
    }
    setRecentlyReconciledPins(nextChangedPins);
  }, [authoritativePinsByPortKey, editablePortKeys, hasExternalMappingAuthority]);
  const effectivePinsByPortKey = useMemo(() => {
    const pins: Record<string, string> = {};
    for (const row of viewModel.pinTable) {
      const portKey = toPortKey(row.port);
      const authoritativePin = normalizePinForExportDisplay(row.pin ?? '');
      // GAP-008 authority rule:
      // - when a parent mapping updater exists, the project prop is the only source of truth;
      // - otherwise, local drafts can drive preview edits for standalone contexts.
      if (hasExternalMappingAuthority || !editablePortKeys.has(portKey)) {
        pins[portKey] = authoritativePin;
        continue;
      }
      pins[portKey] = pinDrafts[portKey] ?? authoritativePin;
    }
    return pins;
  }, [editablePortKeys, hasExternalMappingAuthority, pinDrafts, viewModel.pinTable]);

  const hasBlockingErrors = viewModel.errors.length > 0;
  // When export is hard-blocked, suppress RBEV evidence advisories from the visible list -
  // students should focus on the RBEX blocker, not verify coverage they can't act on yet.
  const visibleDiagnosticsList = hasBlockingErrors
    ? [...viewModel.errors, ...viewModel.warnings]
    : diagnosticsList;
  const hasCircuitFromProject = Boolean(
    (project.circuit?.nodes?.length ?? 0) > 0 || (project.circuit?.connections?.length ?? 0) > 0
  );
  const resolvedWorkflowAuthority = useMemo(
    () =>
      workflowAuthority ??
      deriveProjectWorkflowAuthority({
        projectHealthCore: {
          lastVerify: verifyResult,
          lastExport,
          dirtySinceVerify,
          dirtySinceExport: false,
        },
        readiness: {
          hasCircuit: hasCircuitFromProject,
          hasIoMapping: designReady,
          hasVectors: (activeScenario?.vectors?.length ?? project.vectors?.length ?? 0) > 0,
          hasBlockingDesignIssue: verifyBlockedByDesign,
          blockingDesignIssueMessage: designBlockingIssue?.message,
          verifyQualification: verifyLastRun?.qualification,
        },
        verifyLastRun,
        currentExportHash:
          packageSourceHashes[lastExport?.downloadKind ?? 'project'],
      }),
    [
      activeScenario?.vectors?.length,
      designBlockingIssue?.message,
      designReady,
      dirtySinceVerify,
      hasCircuitFromProject,
      lastExport,
      packageSourceHashes,
      project.vectors?.length,
      verifyLastRun,
      verifyResult,
      verifyBlockedByDesign,
      workflowAuthority,
    ]
  );
  const isIncompleteMappingQualified = resolvedWorkflowAuthority.comparePassIncomplete;
  const verifyState = resolvedWorkflowAuthority.verifyState;
  const hasVerifyPass = resolvedWorkflowAuthority.compareMatches;
  /** True when no verify run has been recorded at all. */
  const isNoRunYet = verifyState === 'not-run';
  const isVerifyStale = verifyState === 'stale';
  const isTraceOnly = resolvedWorkflowAuthority.compareTraceOnly;
  const behavioralEvidenceIsStale = isVerifyStale || dirtySinceVerify;
  const behavioralEvidenceTier = verifyLastRun
    ? deriveBehavioralEvidenceTier(verifyLastRun, behavioralEvidenceIsStale)
    : deriveBehavioralEvidenceTierFromResult(verifyResult, behavioralEvidenceIsStale);
  const hasStaleBehavioralEvidence =
    behavioralEvidenceIsStale && Boolean(verifyLastRun || verifyResult);
  /** True when the previous verify run passed but the circuit has since changed (STALE).
   *  Download is allowed but labeled as previous sealed build - not blocked. */
  const isStaleButPassBefore =
    verifyResult?.status === 'pass' &&
    verifyResult?.runKind !== 'trace' &&
    verifyState === 'stale' &&
    !isIncompleteMappingQualified;
  /** True only when: design is structurally OK, auto-generated (starter) vectors were used,
   *  the run failed, and there are no other hard blockers. Softens export gate for this case. */
  const isStarterScenarioFail =
    !hasBlockingErrors &&
    vectorsAreAutoGenerated &&
    resolvedWorkflowAuthority.compareDiffers &&
    verifyState !== 'stale' &&
    !isIncompleteMappingQualified;
  const hasVerifyEvidenceWarning = evidenceDiagnostics.length > 0;
  const verifyPlain = useMemo(() => {
    if (verifyBlockedByDesign) {
      return 'Inconclusive - Design is structurally blocked, so no saved Compare PASS/FAIL is current.';
    }
    if (hasVerifyPass) return 'Checks match - saved outputs agree with the live design for this build.';
    if (isIncompleteMappingQualified) {
      return 'Checks match, but pin mapping still needs attention before hardware.';
    }
    if (isVerifyStale) return 'Simulation evidence is stale - rerun Simulate after design edits.';
    if (isTraceOnly) return 'Observation-only run — save observed outputs and run Compare checks for trusted export evidence.';
    if (isStarterScenarioFail) return 'Auto-generated vectors only — author your own checks in Simulate for Compare evidence.';
    if (isNoRunYet) {
      return 'Simulation has not run yet — draft export is available, but no Compare evidence exists.';
    }
    if (verifyResult?.status === 'fail') {
      return 'Checks differ - saved outputs do not match the live design.';
    }
    return 'Simulation status indeterminate - open Simulate for details.';
  }, [
    hasVerifyPass,
    isIncompleteMappingQualified,
    isNoRunYet,
    isStarterScenarioFail,
    isTraceOnly,
    isVerifyStale,
    verifyBlockedByDesign,
    verifyResult?.status,
  ]);
  const mappedCount = viewModel.pinTable.filter((row) => {
    const key = toPortKey(row.port);
    const pinValue = (effectivePinsByPortKey[key] ?? '').trim();
    return row.status !== 'unused' && pinValue.length > 0;
  }).length;
  const requiredCount = viewModel.pinTable.filter((r) => r.required).length;
  const requiredMappedCount = viewModel.pinTable.filter((r) => {
    if (!r.required) return false;
    return (effectivePinsByPortKey[toPortKey(r.port)] ?? '').trim().length > 0;
  }).length;
  const mappingPlain = useMemo(() => {
    if (requiredCount === 0) {
      if (mappedCount > 0) {
        return `${mappedCount} mapped board I/O row${mappedCount === 1 ? '' : 's'}; no missing required pins.`;
      }
      return 'No board I/O rows for this export.';
    }
    return `${requiredMappedCount} of ${requiredCount} required ports have Basys3 pin assignments.`;
  }, [mappedCount, requiredCount, requiredMappedCount]);
  // Use diagnostic codes rather than regex-on-message-text so that RBEX3001
  // ("Ignoring source XDC directive "create_clock"…") is never mistaken for a
  // clock-domain blocker. Clock blockers are the RBEX420x clock codes listed here.
  const clockDiag = diagnosticsList.find((d) =>
    d.code === 'RBEX4200' ||
    d.code === 'RBEX4201' ||
    d.code === 'RBEX4202' ||
    d.code === 'RBEX4203' ||
    d.code === 'RBEX4204' ||
    d.code === 'RBEX4206'
  );
  const feedbackDiag = diagnosticsList.find((d) => d.code === 'RBEX4102');
  /** Manual-event labs do not need a board oscillator net; RBEX4200 is advisory, not a screaming blocker. */
  const clockGateIsSoftAdvisory = Boolean(
    clockDiag &&
      activeScheduleContract?.timingMode === 'manual_event_driven_lab' &&
      clockDiag.code === 'RBEX4200'
  );
  const timingStructureOkForHandoff =
    (!clockDiag && !feedbackDiag) || clockGateIsSoftAdvisory;
  const artifactMap = useMemo(
    () => new Map(viewModel.artifacts.map((a) => [a.path.toLowerCase(), a])),
    [viewModel.artifacts]
  );
  const projectMappingMissingRows = useMemo(
    () => viewModel.pinTable.filter((row) => row.required && !editablePortKeys.has(toPortKey(row.port))),
    [editablePortKeys, viewModel.pinTable]
  );
  const unmappedRequiredPorts = useMemo(
    () =>
      viewModel.pinTable.filter(
        (r) => r.required && (effectivePinsByPortKey[toPortKey(r.port)] ?? '').trim().length === 0
      ),
    [effectivePinsByPortKey, viewModel.pinTable]
  );
  const mappingAuthoritySummary =
    'Board & Constraints owns the saved pin binding. Build & Export mirrors that mapping so you can inspect constraints and generated files without editing board state here.';
  const mappingAuthorityUpdateSummary = recentlyReconciledPins.size > 0
    ? `${recentlyReconciledPins.size} pin ${recentlyReconciledPins.size === 1 ? 'was' : 'were'} updated from Board & Constraints.`
    : '';

  const appEnv = (import.meta as ImportMeta & {
    env?: { VITE_APP_VERSION?: string; VITE_GIT_SHA?: string };
  }).env;
  const redbyteVersion = (appEnv?.VITE_APP_VERSION ?? 'dev').trim() || 'dev';
  const redbyteCommit = (appEnv?.VITE_GIT_SHA ?? 'local').trim() || 'local';
  const boardTargetLabel = useMemo(() => {
    const b = project.fpga?.board?.trim();
    if (!b || b.toLowerCase() === 'basys3') return 'Digilent Basys3 (Artix-7)';
    return `FPGA target: ${b}`;
  }, [project.fpga?.board]);
  const gateRows = useMemo(() => {
    const verifyTone = verifyBlockedByDesign
      ? 'error' as const
      : hasVerifyPass
        ? 'ok' as const
      : isIncompleteMappingQualified
        ? 'warn' as const
      : isVerifyStale
        ? 'warn' as const
      : isTraceOnly
        ? 'warn' as const
        : isStarterScenarioFail
          ? 'warn' as const
          : isNoRunYet
            ? 'warn' as const
            : verifyResult
              ? 'error' as const
              : 'warn' as const;
    const verifyDetail = verifyBlockedByDesign
      ? 'Inconclusive - Design blocked'
      : hasVerifyPass
        ? `Complete - ${verifyResult?.hash?.slice(0, 8) ?? ''}`
      : isIncompleteMappingQualified
        ? 'Pass incomplete - mapping'
      : isVerifyStale
        ? 'Stale - rerun Simulate'
        : isStarterScenarioFail
          ? 'Starter scenario only'
          : isNoRunYet
            ? 'Not run yet'
            : verifyResult
              ? typeof verifyResult.failingTick === 'number'
                ? `Outputs differ - t${verifyResult.failingTick}`
                : 'Outputs differ'
              : 'Not run';
    const verifyDetailLabel = isTraceOnly ? 'Trace only' : verifyDetail;
    const mappingTone: 'ok' | 'error' =
      requiredCount === 0 || requiredMappedCount === requiredCount ? 'ok' : 'error';
    const mappingDetail =
      requiredCount === 0 && viewModel.pinTable.length > 0
        ? `${mappedCount}/${viewModel.pinTable.length} mapped`
        : `${requiredMappedCount}/${requiredCount} required`;
    const clockTone =
      feedbackDiag || (clockDiag && !clockGateIsSoftAdvisory)
        ? ('error' as const)
        : clockGateIsSoftAdvisory
          ? ('warn' as const)
          : 'ok' as const;
    const clockDetail = feedbackDiag
      ? 'Unsupported feedback loop'
      : clockGateIsSoftAdvisory
        ? 'Lab timing: no board oscillator required - open details only if you add a real board clock.'
        : clockDiag
          ? clockDiag.message.slice(0, 55)
          : formatExportClockGateDetail(activeScheduleContract, effectiveTimingGuidance.exportDetail);
    const mappingGateActionLabel = mappingTone === 'error' ? 'Open Board & Constraints' : 'Review mapping';
    return [
      {
        id: 'verify',
        label: 'Simulate',
        tone: verifyTone,
        detail: verifyDetailLabel,
        actionLabel: verifyBlockedByDesign ? 'Open Design' : verifyResult ? 'Re-run' : 'Run Simulate',
        onAction: verifyBlockedByDesign ? onGoToDesign : onOpenVerify ?? undefined,
      },
      {
        id: 'mapping',
        label: 'I/O Mapping',
        tone: mappingTone,
        detail: mappingDetail,
        actionLabel: mappingGateActionLabel,
        onAction: () => {
          if (onGoToHardware) {
            onGoToHardware();
            return;
          }
          mapSectionRef.current?.scrollIntoView({ behavior: 'smooth', block: 'start' });
        },
      },
      {
        id: 'clock',
        label: feedbackDiag ? 'State structure' : effectiveTimingGuidance.exportLabel,
        tone: clockTone,
        detail: clockDetail,
        actionLabel: 'Details',
        onAction: feedbackDiag
          ? () => setOpenFixPathId((prev) => (prev === feedbackDiag.id ? null : feedbackDiag.id))
          : clockDiag
            ? () => setOpenFixPathId((prev) => (prev === clockDiag.id ? null : clockDiag.id))
          : undefined,
      },
    ];
  }, [
    activeScheduleContract,
    clockGateIsSoftAdvisory,
    effectiveTimingGuidance.exportDetail,
    effectiveTimingGuidance.exportLabel,
    feedbackDiag,
    hasVerifyPass,
    isIncompleteMappingQualified,
    isVerifyStale,
    verifyBlockedByDesign,
    verifyResult,
    isStarterScenarioFail,
    isNoRunYet,
    requiredMappedCount,
    requiredCount,
    mappedCount,
    viewModel.pinTable.length,
    clockDiag,
    hasBlockingErrors,
    diagnosticsList,
    onGoToDesign,
    onGoToHardware,
    onOpenVerify,
    hasExternalMappingAuthority,
    onGoToProject,
  ]);

  const deterministicChecks = useMemo(() => [
    {
      id: 'clock',
      label: feedbackDiag
        ? 'Supported state structure'
        : effectiveTimingGuidance.kind === 'latch-control'
          ? 'Supported latch control'
          : 'Single clock domain',
      tooltip: feedbackDiag
        ? 'Unsupported feedback loops must be rewritten as supported latches or flip-flops before export.'
        : clockGateIsSoftAdvisory
          ? 'Manual-event lab: export does not require mapping the board oscillator. Resolve only if you intend to use the Basys3 clock pin.'
          : formatExportClockDeterministicTooltip(activeScheduleContract, effectiveTimingGuidance.exportTooltip),
      pass: timingStructureOkForHandoff,
    },
    {
      id: 'floating',
      label: 'No floating drivers',
      tooltip: 'No output ports are left unconnected. Floating outputs cannot be synthesized correctly.',
      pass: !diagnosticsList.some((d) => d.code === 'RBEX4103'),
    },
    {
      id: 'pins',
      label: 'All mapped pins bound',
      tooltip: 'Every declared I/O port has a Basys3 pin assignment. Unmapped ports block the constraints file.',
      pass: requiredCount === 0 || requiredMappedCount >= requiredCount,
    },
    {
      id: 'verify',
      label: 'Simulation evidence embedded',
      tooltip: 'A passing Compare run has been completed for this exact circuit. Open Simulate, run with checks enabled, and reach PASS to satisfy this.',
      pass: hasVerifyPass,
    },
  ], [activeScheduleContract, clockDiag, clockGateIsSoftAdvisory, diagnosticsList, effectiveTimingGuidance.exportTooltip, effectiveTimingGuidance.kind, feedbackDiag, requiredMappedCount, requiredCount, hasVerifyPass, timingStructureOkForHandoff]);
  const artifactAgreementRows = useMemo(
    () =>
      buildArtifactAgreementRows({
        project,
        viewModel,
        requiredMappedCount,
        requiredCount,
        timingStructureOk: timingStructureOkForHandoff,
        topVhdStatus: pickArtifactStatus(artifactMap, 'top.vhd'),
        topXdcStatus: pickArtifactStatus(artifactMap, 'top.xdc'),
        testbenchStatus: pickArtifactStatus(artifactMap, 'testbench.vhd'),
        readmeStatus: pickArtifactStatus(artifactMap, 'readme.txt'),
        vivadoScriptStatus: pickArtifactStatus(artifactMap, 'vivado_import.tcl'),
      }),
    [
      artifactMap,
      clockDiag,
      feedbackDiag,
      timingStructureOkForHandoff,
      project,
      requiredCount,
      requiredMappedCount,
      viewModel,
    ]
  );
  const agreementWorst = useMemo(
    () => worstAgreementTone(artifactAgreementRows),
    [artifactAgreementRows]
  );
  const selectedArtifact =
    viewModel.artifacts.find((artifact) => artifact.path === selectedArtifactPath) ??
    viewModel.artifacts[0];
  const artifactGroups = useMemo(
    () => buildArtifactGroups(viewModel.artifacts),
    [viewModel.artifacts]
  );
  const keyArtifacts = useMemo(
    () => ({
      topVhd: artifactMap.get('top.vhd'),
      topXdc: artifactMap.get('top.xdc'),
      vivadoImport: artifactMap.get('vivado_import.tcl'),
    }),
    [artifactMap]
  );
  const readyArtifactCount = useMemo(
    () => viewModel.artifacts.filter((artifact) => artifact.status === 'ready').length,
    [viewModel.artifacts]
  );
  const primaryPackageArtifacts = useMemo(
    () =>
      FIRST_VIEWPORT_ARTIFACT_PATHS
        .map((path) => artifactMap.get(path.toLowerCase()))
        .filter((artifact): artifact is ExportArtifactView => Boolean(artifact)),
    [artifactMap]
  );
  const mappingBlockingDiagnostic = viewModel.errors.find(
    (entry) =>
      entry.code === 'RBEX1001' ||
      entry.code === 'RBEX1002' ||
      entry.code === 'RBEX2001' ||
      entry.code === 'RBEX2002' ||
      entry.code === 'RBEX2003' ||
      /duplicate pin assignment|package pin .* used by/i.test(entry.message)
  );
  const hasMappingError = Boolean(mappingBlockingDiagnostic);
  const hasRequiredMappingGap =
    unmappedRequiredPorts.length > 0 ||
    hasMappingError ||
    (!resolvedWorkflowAuthority.designReady && requiredCount > 0 && requiredMappedCount < requiredCount);
  const hasOtherBlockingIssue =
    (hasBlockingErrors && !hasMappingError) ||
    (!resolvedWorkflowAuthority.designReady && !hasRequiredMappingGap);
  const handoffTruth = useMemo(
    () =>
      deriveHardwareExportFailureTruth({
        workflowAuthority: resolvedWorkflowAuthority,
        hasRequiredMappingGap,
        hasOtherBlockingIssue,
      }),
    [hasOtherBlockingIssue, hasRequiredMappingGap, resolvedWorkflowAuthority]
  );
  const exportBlocked = handoffTruth.severity === 'blocked';
  const liveVerificationTrust = deriveExportVerificationTrust({
    verifyState,
    compareMatches: resolvedWorkflowAuthority.compareMatches,
    comparePassIncomplete: resolvedWorkflowAuthority.comparePassIncomplete,
  });
  const isReceiptCurrent = (receipt?: ProjectHealthExportResult): boolean => {
    const downloadKind = receipt?.downloadKind ?? 'project';
    return !exportBlocked && isExportDownloadReceiptCurrent({
      receipt,
      currentPackageSourceHash: packageSourceHashes[downloadKind],
      currentProjectHash: determinismHash,
      currentVerifyHash: verifyResult?.hash,
      currentVerificationTrust: liveVerificationTrust,
      downloadKind,
    });
  };
  const sessionDownloadIsCurrent = isReceiptCurrent(sessionDownloadEvidence);
  const persistedDownloadIsCurrent = isReceiptCurrent(lastExport);
  const currentDownloadEvidence = sessionDownloadIsCurrent
    ? sessionDownloadEvidence
    : persistedDownloadIsCurrent
      ? lastExport
      : undefined;
  const downloadDone = Boolean(currentDownloadEvidence);
  const lastDownloadKind = currentDownloadEvidence?.downloadKind ?? null;
  const exportTrustAxes = deriveExportTrustAxes({
    structuralBlocked: exportBlocked,
    verifyState,
    compareMatches: resolvedWorkflowAuthority.compareMatches,
    comparePassIncomplete: resolvedWorkflowAuthority.comparePassIncomplete,
    downloadedTrust: currentDownloadEvidence?.verificationTrust,
  });
  const downloadReady = exportTrustAxes.structural === 'downloadable';
  const exportTrusted = currentDownloadEvidence?.verificationTrust === 'trusted';
  const isDraftExport =
    downloadReady && exportTrustAxes.verificationTrust !== 'trusted';
  const packageHandoffSummary = useMemo(
    () =>
      derivePackageHandoffSummary({
        handoffSeverity: handoffTruth.severity,
        handoffCondition: handoffTruth.condition,
        exportViewBlocked: viewModel.status === 'blocked',
        hasVerifyEvidenceWarning,
        agreementWorst,
        boardTarget: boardTargetLabel,
        timingPlain,
        mappingPlain,
        verifyPlain,
      }),
    [
      agreementWorst,
      boardTargetLabel,
      handoffTruth.condition,
      handoffTruth.severity,
      hasVerifyEvidenceWarning,
      mappingPlain,
      timingPlain,
      verifyPlain,
      viewModel.status,
    ]
  );
  const handoffTone: 'ok' | 'warn' | 'error' =
    handoffTruth.severity === 'ready'
      ? 'ok'
      : handoffTruth.severity === 'blocked'
        ? 'error'
        : 'warn';
  const trustReason = useMemo(() => {
    switch (handoffTruth.condition) {
      case 'mapping-incomplete':
        return unmappedRequiredPorts.length > 0
          ? `${unmappedRequiredPorts.length} required port${unmappedRequiredPorts.length === 1 ? '' : 's'} still need Basys3 pin assignments.`
          : mappingBlockingDiagnostic?.title ?? 'Required pin mapping is still incomplete.';
      case 'design-blocked':
        return verifyBlockedByDesign
          ? `${designBlockingIssue?.message ?? 'The current design is structurally blocked.'} Compare evidence is inconclusive until Design is repaired.`
          : viewModel.errors.find((entry) => entry.code !== 'RBEX1001' && entry.code !== 'RBEX1002')?.title
            ?? 'The current design still has blocking export diagnostics.';
      case 'export-stale':
        return 'The last successful export bundle no longer matches the current circuit.';
      case 'export-missing':
        return 'No successful export bundle has been recorded for this circuit yet.';
      case 'verify-not-run':
        return 'Expected-output comparison has not run for this circuit yet.';
      case 'verify-stale':
        return 'The last simulation evidence is stale for the current design, testbench, or mapping.';
      case 'assertions-differ':
        return evidenceDiagnostics[0]?.message ?? 'Assertions differ from observed outputs.';
      case 'trace-only':
        return 'The current simulation evidence is from a trace-only run.';
      case 'mapping-review':
        return 'The latest comparison passed, but mapping review is still incomplete.';
      case 'ready':
        return 'The current export bundle and current comparison evidence agree.';
      default:
        return handoffTruth.title;
    }
  }, [
    evidenceDiagnostics,
    designBlockingIssue?.message,
    handoffTruth.condition,
    handoffTruth.title,
    mappingBlockingDiagnostic?.title,
    unmappedRequiredPorts.length,
    verifyBlockedByDesign,
    viewModel.errors,
  ]);

    /** Surface status title: names the current export tier in the command strip. */
    const surfaceStatusTitle = useMemo(() => {
      if (exportTrustAxes.derived === 'blocked') {
        return handoffTruth.condition === 'mapping-incomplete'
          ? 'Mapping blocks export'
          : 'Design blocks export';
      }
      if (exportTrustAxes.action === 'downloaded') {
        return `${formatExportDerivedState(exportTrustAxes.derived)} package`;
      }
      if (handoffTruth.condition === 'mapping-review') {
        return 'E0 export package ready - mapping review pending';
      }
      if (exportTrustAxes.verificationTrust === 'unverified') {
        return 'Draft export available - unverified';
      }
      if (exportTrustAxes.verificationTrust === 'draft') {
        return 'Draft export available';
      }

      switch (handoffTruth.condition) {
        case 'export-stale':
          return 'Package stale — rebuild to match the current design';
        case 'export-missing':
          return 'Package not built yet — evidence validated';
        case 'mapping-review':
          return 'Package ready — mapping review pending';
        case 'ready':
          return 'Package current';
        default:
          return 'Export status';
      }
    }, [exportTrustAxes, handoffTruth.condition]);

    const surfaceStatusDetail = useMemo(() => {
      if (downloadDone) {
        return `${formatExportTrustDetail(exportTrustAxes.verificationTrust)} Unzip the ZIP, then open the .xpr file in Vivado.`;
      }

      switch (handoffTruth.condition) {
        case 'export-stale':
        case 'export-missing':
        case 'ready':
          return `${handoffTruth.message} This is E0 export-package evidence only; Vivado build, board programming, and observed behavior remain external.`;
        default:
          return handoffTruth.message;
      }
    }, [downloadDone, exportTrustAxes.verificationTrust, handoffTruth.condition, handoffTruth.message]);

  const primaryExportActionDownloadsProject =
    handoffTruth.primaryCtaIntent === 'build-current-bundle' ||
    handoffTruth.primaryCtaIntent === 're-export-current-bundle' ||
    handoffTruth.primaryCtaIntent === 'program-handoff';
  const primaryHandoffDisabled =
    primaryExportActionDownloadsProject
      ? (!downloadReady || isRebuilding)
      : (handoffTruth.primaryCtaIntent === 'map-pins' && !onGoToHardware) ||
        (handoffTruth.primaryCtaIntent === 'design' && !onGoToDesign) ||
        (handoffTruth.primaryCtaIntent === 'verify' && !onOpenVerify);
  const vivadoCommand =
    'vivado -mode batch -source vivado_import.tcl -notrace -nojournal -log vivado_import.log';
  const projectDownloadCompactLabel = isRebuilding
    ? 'Building...'
    : handoffTruth.primaryCtaIntent === 'build-current-bundle'
      ? 'Build Current Bundle'
      : handoffTruth.primaryCtaIntent === 're-export-current-bundle'
        ? 'Rebuild Current Bundle'
    : downloadDone && lastDownloadKind === 'project'
      ? 'Re-download'
      : isDraftExport
        ? 'Download Draft Project ZIP'
      : isStaleButPassBefore
        ? 'Download Project ZIP (previous build)'
        : isStarterScenarioFail
          ? 'Download Project ZIP (starter)'
          : 'Download Project ZIP';
  const primaryExportCtaLabel =
    handoffTruth.primaryCtaIntent === 'program-handoff'
      ? projectDownloadCompactLabel
      : handoffTruth.primaryCtaLabel;
  const gateStackSection = (
    <div className="ide-export-gate-details-panel" data-testid="ide-export-readiness-details">
      <header className="ide-export-section-header">
        <div>
          <h3>Readiness gates</h3>
          <p className="ide-export-section-subcopy">
            Expand the gate readout only when you need the exact reason Export is ready, advisory, or blocked.
          </p>
        </div>
      </header>
      <div className="ide-export-gate-stack" data-testid="ide-export-gate-stack">
        {gateRows.map((gate) => (
          <div
            key={gate.id}
            className={`ide-export-gate-row ${
              gate.tone === 'ok' ? 'is-pass' : gate.tone === 'warn' ? 'is-warn' : 'is-fail'
            }`}
            data-testid={`ide-export-gate-${gate.id}`}
          >
            <IdeStatusPill tone={gate.tone}>
              {gate.tone === 'ok'
                ? 'READY'
                : gate.tone === 'warn'
                  ? gate.id === 'clock' && clockGateIsSoftAdvisory
                    ? 'ADVISORY'
                    : 'REVIEW'
                  : 'NEEDS FIX'}
            </IdeStatusPill>
            <span className="ide-export-gate-label">{gate.label}</span>
            <span className="ide-export-gate-detail">{gate.detail}</span>
            {gate.onAction && (
              <IdeButton
                tone="ghost"
                onClick={gate.onAction}
                testId={`ide-export-gate-action-${gate.id}`}
              >
                {gate.actionLabel}
              </IdeButton>
            )}
          </div>
        ))}
      </div>
    </div>
  );
  const quickDebugReport = useMemo(() => {
    const mappingLines = [...viewModel.pinTable]
      .map((row) => {
        const portKey = toPortKey(row.port);
        const pinValue = (effectivePinsByPortKey[portKey] ?? row.pin ?? '').trim();
        const resolvedPin = pinValue.length > 0 ? formatLabelFirstPinBinding(pinValue) : 'UNMAPPED';
        const requiredTag = row.required ? ' required' : ' optional';
        return `${row.port} (${row.direction}, ${row.status}${requiredTag}) -> ${resolvedPin}`;
      })
      .sort((left, right) => left.localeCompare(right));

    return [
      'RedByte Vivado Quick Debug Report',
      `project=${project.name}`,
      `board=basys3`,
      `redbyteVersion=${redbyteVersion}`,
      `redbyteCommit=${redbyteCommit}`,
      `exportHash=${viewModel.exportHash ?? 'pending'}`,
      `verifyHash=${verifyResult?.hash ?? 'pending'}`,
      '',
      'mapping:',
      ...mappingLines,
    ].join('\n');
  }, [
    effectivePinsByPortKey,
    project.name,
    redbyteCommit,
    redbyteVersion,
    verifyResult?.hash,
    viewModel.exportHash,
    viewModel.pinTable,
  ]);
  const jumpToMapping = (portKey: string) => {
    const row = rowRefs.current[portKey];
    row?.scrollIntoView({ behavior: 'smooth', block: 'center' });
    setHighlightedPort(portKey);
    if (highlightResetTimer.current !== null && typeof window !== 'undefined') {
      window.clearTimeout(highlightResetTimer.current);
    }
    if (typeof window !== 'undefined') {
      highlightResetTimer.current = window.setTimeout(() => {
        setHighlightedPort(null);
      }, 1200);
    }
  };

  // ─── Phase 32: Pipeline helpers ────────────────────────────────────────────
  const markStep = useCallback((id: RebuildStepId, state: RebuildStepState, detail?: string) => {
    setRebuildSteps((prev) =>
      prev.map((s) => (s.id === id ? { ...s, state, detail } : s))
    );
  }, []);

  const resetSteps = useCallback(() => {
    setRebuildSteps(makeSteps());
    setIsRebuilding(false);
  }, []);

  const handleDownloadExport = useCallback(async (kind: 'project' | 'kit') => {
    setIsRebuilding(true);
    resetSteps();
    setDownloadError('');
    const ranAtIso = new Date().toISOString();

    // re-init steps
    setRebuildSteps(makeSteps());

    try {
      // STEP: validate — check blocking diagnostics
      markStep('validate', 'running');
      await tick();
      if (hasBlockingErrors) {
        markStep('validate', 'error', 'Blocking diagnostics present');
        setDownloadError(
          kind === 'project'
            ? 'Resolve blocking diagnostics before downloading the Vivado Project.'
            : 'Resolve blocking diagnostics before downloading the Vivado Kit.'
        );
        setIsRebuilding(false);
        return;
      }
      markStep('validate', 'done');

      // STEP: mapping — check required pins covered
      markStep('mapping', 'running');
      await tick();
      if (requiredMappedCount < requiredCount) {
        markStep('mapping', 'error', `${requiredCount - requiredMappedCount} required pin${requiredCount - requiredMappedCount !== 1 ? 's' : ''} unmapped`);
        setDownloadError(`${requiredCount - requiredMappedCount} required pin${requiredCount - requiredMappedCount !== 1 ? 's' : ''} unmapped.`);
        setIsRebuilding(false);
        return;
      }
      markStep('mapping', 'done');

      // STEP: clock — record verify/trust state.
      // Export package generation is allowed whenever the export itself is structurally valid.
      // Verify seals trust; it does not determine whether current compiler artifacts exist.
      markStep('clock', 'running');
      await tick();
      const isStarterFailNow =
        vectorsAreAutoGenerated &&
        verifyResult?.status === 'fail' &&
        !dirtySinceVerify &&
        verifyLastRun?.qualification !== 'incomplete-mapping';
      const isVerifyStaleNow = Boolean(verifyResult && dirtySinceVerify);
      const isNoRunYetNow = !verifyResult;
      const isVerifyMismatchNow = verifyResult?.status === 'fail';
      const isIncompleteMappingNow = verifyLastRun?.qualification === 'incomplete-mapping';
      if (isStarterFailNow) {
        markStep('clock', 'warn', 'Starter scenario only');
      } else if (isNoRunYetNow) {
        markStep('clock', 'warn', 'Simulate not run yet');
      } else if (isVerifyStaleNow) {
        markStep('clock', 'warn', 'Simulate needs rerun');
      } else if (isIncompleteMappingNow) {
        markStep('clock', 'warn', 'Mapping incomplete');
      } else if (isVerifyMismatchNow) {
        markStep('clock', 'warn', 'Simulation mismatch');
      } else {
        markStep('clock', 'done');
      }

      // STEP: bundle — assemble VHDL + constraints
      markStep('bundle', 'running');
      await tick();
      markStep('bundle', 'done');

      // STEP: manifest — build README
      markStep('manifest', 'running');
      await tick();
      markStep('manifest', 'done');

      // STEP: zip — package and download
      markStep('zip', 'running');
      const zipBytes =
        kind === 'project'
          ? await buildVivadoProjectFolderZip({
              artifacts: viewModel.artifacts.map((artifact) => ({
                path: artifact.path,
                content: artifact.content,
              })),
              projectName: project.name,
              projectSlug,
              topModule,
              part: vivadoPart,
            })
          : await buildVivadoKitZip(viewModel.artifacts);
      // Hash the finished bytes after generation. The evidence record must never
      // participate in package construction or change deterministic ZIP content.
      const packageHash = await sha256Hex(zipBytes);
      downloadZipBytes(
        zipBytes,
        kind === 'project' ? `${projectSlug}-vivado-project.zip` : 'redbyte-vivado-kit.zip'
      );
      markStep('zip', 'done');

      onExportBundle?.(viewModel.artifacts);
      const exportResult: ProjectHealthExportResult = {
        status: 'ok',
        hash: packageSourceHashes[kind],
        bundleHash: packageHash,
        packageHash,
        verificationTrust: exportTrustAxes.verificationTrust,
        downloadKind: kind,
        downloadedAtIso: ranAtIso,
        sourceHashes: {
          project: determinismHash,
          export: packageSourceHashes[kind],
          verify: verifyResult?.hash,
        },
        sourceCurrentness: {
          project: 'current',
          export: 'current',
          mapping: 'current',
          verify: deriveExportVerifySourceState(
            verifyState,
            resolvedWorkflowAuthority.comparePassIncomplete
          ),
        },
        artifacts: viewModel.artifacts.map((a) => a.path),
        ranAtIso,
      };
      setSessionDownloadEvidence(exportResult);
      onExportResult?.(exportResult);
    } catch (error) {
      const reason =
        error instanceof Error && error.message.trim().length > 0
          ? error.message.trim()
          : 'unknown build error';
      setDownloadError(`Build failed: ${reason}`);
      setRebuildSteps((prev) =>
        prev.map((s) => (s.state === 'running' ? { ...s, state: 'error', detail: reason } : s))
      );
      onExportResult?.({
        status: 'blocked',
        hash: packageSourceHashes.project,
        artifacts: viewModel.artifacts.map((a) => a.path),
        ranAtIso,
      });
    } finally {
      setIsRebuilding(false);
    }
  }, [
    resetSteps, markStep, hasBlockingErrors, requiredMappedCount, requiredCount,
    verifyResult, dirtySinceVerify, viewModel, onExportBundle, onExportResult,
    project.name, projectSlug, topModule, vivadoPart, determinismHash,
    packageSourceHashes,
    exportTrustAxes.verificationTrust, resolvedWorkflowAuthority.comparePassIncomplete,
    verifyState,
  ]);
  const handlePrimaryHandoff = useCallback(() => {
    switch (handoffTruth.primaryCtaIntent) {
      case 'build-current-bundle':
      case 're-export-current-bundle':
        void handleDownloadExport('project');
        break;
      case 'map-pins':
        if (onGoToHardware) onGoToHardware();
        else onGoToProject?.();
        break;
      case 'design':
        onGoToDesign?.();
        break;
      case 'verify':
        onOpenVerify?.();
        break;
      case 'program-handoff':
        void handleDownloadExport('project');
        break;
      default:
        break;
    }
  }, [handoffTruth.primaryCtaIntent, handleDownloadExport, onGoToDesign, onGoToHardware, onGoToProject, onOpenVerify]);

  const handleDownloadArtifact = (artifact: ExportArtifactView) => {
    if (typeof window === 'undefined' || artifact.content.trim().length === 0) return;
    const blob = new Blob([artifact.content], { type: 'text/plain;charset=utf-8' });
    const url = URL.createObjectURL(blob);
    const link = document.createElement('a');
    link.href = url;
    link.download = artifact.path;
    document.body.appendChild(link);
    link.click();
    document.body.removeChild(link);
    window.setTimeout(() => URL.revokeObjectURL(url), 0);
  };

  const copyToClipboard = async (payload: string, targetId: string) => {
    if (typeof navigator === 'undefined' || !navigator.clipboard?.writeText) {
      setCopiedTarget(null);
      setCopyError(true);
      return;
    }
    try {
      await navigator.clipboard.writeText(payload);
      setCopiedTarget(targetId);
      setCopyError(false);
      if (copyResetTimer.current !== null && typeof window !== 'undefined') {
        window.clearTimeout(copyResetTimer.current);
      }
      if (typeof window !== 'undefined') {
        copyResetTimer.current = window.setTimeout(() => {
          setCopiedTarget(null);
          setCopyError(false);
        }, 1600);
      }
    } catch {
      setCopiedTarget(null);
      setCopyError(true);
    }
  };

  // ── Readiness evidence used by the single Export decision header ────────────
  const verifyProvenance: ExportVerifyProvenance = verifyBlockedByDesign
    ? 'design-blocked'
    : !verifyResult
      ? 'not-run'
      : dirtySinceVerify
        ? 'stale'
      : verifyState === 'trace'
        ? 'trace'
        : verifyResult.status === 'pass'
          ? 'pass'
          : 'fail';

  const downloadDisabled = !downloadReady || isRebuilding;
  const vivadoEvidenceRows = [
    {
      id: 'e0',
      level: 'E0',
      label: 'Export package',
      tone: hasBlockingErrors ? 'error' as const : 'ok' as const,
      status: hasBlockingErrors
        ? 'Blocked'
        : downloadDone
          ? 'Downloaded'
          : 'Generated in RedByte',
      detail: hasBlockingErrors
        ? 'Resolve export blockers before the package can be considered export-valid.'
        : 'RedByte generated the VHDL, XDC, Tcl, README, and project handoff artifacts. This is export/package evidence only.',
    },
    {
      id: 'e1',
      level: 'E1',
      label: 'Vivado build / bitstream',
      tone: 'warn' as const,
      status: 'External evidence required',
      detail: 'Run Vivado synthesis, implementation, and bitstream generation outside RedByte. Preserve and classify warnings before claiming E1.',
    },
    {
      id: 'e2',
      level: 'E2',
      label: 'Board programming',
      tone: 'warn' as const,
      status: 'External evidence required',
      detail: 'Program success proves delivery to the board only; E2 does not prove behavior and must not be treated as E3.',
    },
    {
      id: 'e3',
      level: 'E3',
      label: 'Observed board behavior',
      tone: 'warn' as const,
      status: 'manual observation required',
      detail: 'Toggle the expected controls and record physical LED/output behavior before making any behavior-certified claim.',
    },
  ];
  const exportConfidenceReady =
    exportTrusted ||
    handoffTruth.condition === 'export-missing' ||
    handoffTruth.condition === 'ready';
  const exportConfidencePill = exportTrusted
    ? 'Trusted E0'
    : handoffTruth.condition === 'export-missing'
      ? 'Ready E0'
      : exportBlocked
        ? 'Blocked'
        : 'Draft';
  const exportConfidenceRows = useMemo(() => {
    const verifyTone =
      verifyBlockedByDesign
        ? ('error' as const)
        : hasVerifyPass
        ? ('ok' as const)
        : verifyResult?.status === 'fail'
          ? ('error' as const)
          : ('warn' as const);
    const verifyStatus = verifyBlockedByDesign
      ? 'Inconclusive - Design blocked'
      : hasVerifyPass
        ? 'Current Compare PASS'
      : isVerifyStale
        ? 'Stale - rerun Compare'
        : isTraceOnly
          ? 'Observe only - no Compare proof'
          : verifyResult?.status === 'fail'
            ? 'Compare FAIL'
            : 'Not run';
    const verifyDetail = verifyBlockedByDesign
      ? designBlockingIssue?.message ?? 'Repair the structural Design issue before relying on Compare evidence.'
      : hasVerifyPass
        ? 'Saved expected outputs matched observed outputs for this current design hash.'
      : isVerifyStale
        ? 'The design, testbench, or mapping changed after the last trusted run.'
        : verifyResult?.status === 'fail'
          ? 'Fix expected outputs or the design before treating this package as trusted.'
          : verifyPlain;
    const mappingReady = requiredCount === 0 || requiredMappedCount === requiredCount;
    const packageStatus = exportTrusted
      ? 'Trusted E0 package'
      : handoffTruth.condition === 'export-missing'
        ? 'Ready to build E0 package'
        : exportBlocked
        ? 'Blocked draft'
        : isDraftExport
          ? 'Draft package - not trusted'
          : 'Needs rebuild';
    const packageDetail = exportTrusted
      ? 'The current browser-E0 bundle matches simulation evidence and mapping; Vivado build, bitstream, and board behavior remain external.'
      : handoffTruth.condition === 'export-missing'
        ? 'Simulation evidence and mapping are current enough to build the browser-E0 ZIP; the ZIP is not Vivado or board proof.'
      : 'Files may be inspectable, but this is not a current trusted handoff yet.';

    return [
      {
        id: 'verify',
        label: 'Simulation evidence',
        status: verifyStatus,
        detail: verifyDetail,
        tone: verifyTone,
      },
      {
        id: 'mapping',
        label: 'Pin mapping',
        status: mappingReady ? 'Mapping current' : 'Mapping missing',
        detail: mappingPlain,
        tone: mappingReady ? ('ok' as const) : ('error' as const),
      },
      {
        id: 'package',
        label: 'Package trust',
        status: packageStatus,
        detail: packageDetail,
        tone: exportConfidenceReady ? ('ok' as const) : exportBlocked ? ('error' as const) : ('warn' as const),
      },
      {
        id: 'vivado',
        label: 'Vivado build',
        status: 'Not run in RedByte',
        detail: 'Run synthesis, implementation, and bitstream generation outside RedByte before claiming E1.',
        tone: 'warn' as const,
      },
      {
        id: 'board',
        label: 'Board behavior',
        status: 'Not observed',
        detail: 'Physical Basys3 behavior still requires manual board observation before claiming E3.',
        tone: 'warn' as const,
      },
    ];
  }, [
    exportBlocked,
    exportConfidenceReady,
    exportTrusted,
    designBlockingIssue?.message,
    hasVerifyPass,
    handoffTruth.condition,
    isDraftExport,
    isTraceOnly,
    isVerifyStale,
    mappingPlain,
    requiredCount,
    requiredMappedCount,
    verifyPlain,
    verifyBlockedByDesign,
    verifyResult?.status,
  ]);
  const vivadoWarningClasses = [
    'expected/no-clock/combinational',
    'needs RedByte explanation',
    'build blocker',
    'programming blocker',
    'observation blocker',
  ];

  const partNumberCopied = copiedTarget === 'part-number';
  const debugReportCopyState: ExportDebugCopyState =
    copiedTarget === 'report' ? 'copied' : copyError ? 'error' : 'idle';
  const blockedRecovery = (() => {
    switch (handoffTruth.primaryCtaIntent) {
      case 'verify':
        return {
          owner: 'Simulate',
          label: 'Open Simulate',
          action: onOpenVerify,
          testId: 'ide-export-blocked-open-verify',
        };
      case 'design':
        return {
          owner: 'Design',
          label: 'Open Design',
          action: onGoToDesign,
          testId: 'ide-export-blocked-open-design',
        };
      case 'map-pins':
      default:
        return {
          owner: 'Board & Constraints',
          label: 'Open Board & Constraints',
          action: onGoToHardware ?? onGoToProject,
          testId: 'ide-export-blocked-open-map-pins',
        };
    }
  })();
  const designStageReady = hasCircuitFromProject && !hasOtherBlockingIssue;
  const verifyStageReady = hasVerifyPass;
  const mappingStageReady =
    (requiredCount === 0 || requiredMappedCount === requiredCount) && !hasMappingError;
  const upstreamReadinessRows = [
    {
      id: 'design',
      owner: 'Design',
      ready: designStageReady,
      status: designStageReady ? 'Ready' : hasCircuitFromProject ? 'Blocked' : 'No circuit',
      detail: designStageReady
        ? 'The current logical design can be packaged.'
        : 'Open Design to repair the circuit or top-level I/O boundary before Export.',
    },
    {
      id: 'verify',
      owner: 'Simulate',
      ready: behavioralEvidenceTier === 'validated',
      status: verifyBlockedByDesign
        ? 'Inconclusive - Design blocked'
        : hasStaleBehavioralEvidence
          ? 'Stale - rerun Simulate'
          : formatBehavioralEvidenceTier(behavioralEvidenceTier),
      detail:
        behavioralEvidenceTier === 'validated'
          ? 'The current simulation completed and all configured optional checks passed.'
          : hasStaleBehavioralEvidence
            ? 'Prior simulation evidence exists, but the design, testbench, or mapping changed. Rerun the current scenario in Simulate before relying on this handoff.'
            : behavioralEvidenceTier === 'simulated'
              ? verifyResult?.status === 'fail'
                ? 'Simulation completed, but one or more expected outputs differ from the observed circuit behavior.'
                : 'A current simulation trace exists. Optional checks are absent or not all passing.'
              : 'Run the current scenario in Simulate to create behavioral evidence.',
    },
    {
      id: 'mapping',
      owner: 'Board & Constraints',
      ready: mappingStageReady,
      status: mappingStageReady
        ? 'Ready'
        : unmappedRequiredPorts.length > 0
          ? `${unmappedRequiredPorts.length} required missing`
          : 'Resolve mapping blocker',
      detail: mappingStageReady
        ? mappingPlain
        : unmappedRequiredPorts.length > 0
          ? 'Open Board & Constraints to assign one Basys3 resource and package pin to every required signal.'
          : mappingBlockingDiagnostic?.message ?? 'Open Board & Constraints to resolve the saved assignment blocker.',
    },
  ];

  return (
    <IdeSurfaceLayout
      mode="export"
      layoutIntent="workbench"
      consoleHasBlocking={hasBlockingErrors}
      consoleHasEntries={diagnosticsList.length > 0}
      leftDockMode="hidden"
      rightDockMode="hidden"
      consoleMode={problemsLedgerCount > 0 ? 'collapsed' : 'hidden'}
      console={<ProblemsPanel origin="bottom-panel" />}
      productSpine={{
        currentStage: 'Build & Export',
        nextStage: exportBlocked
          ? blockedRecovery.owner
          : downloadDone
            ? 'Vivado'
            : 'Download package',
        status: formatExportDerivedState(exportTrustAxes.derived),
        owner: exportBlocked ? blockedRecovery.owner : 'Build & Export',
        title: surfaceStatusTitle,
        detail: surfaceStatusDetail,
      }}
    >
      <IdePanel testId="ide-export-panel">
        <section
          ref={surfaceRef}
          className="rb-pkg rb-pkg-hero"
          data-testid="ide-export-readiness-hero"
          aria-label="Build and export package decision and files"
        >
          <section
            className={
              'rb-pkg-decision' +
              (exportBlocked
                ? ' is-blocked'
                : exportTrustAxes.verificationTrust === 'trusted'
                  ? ' is-ready'
                  : ' is-draft')
            }
            data-testid="ide-export-package-inspector-v1"
            data-export-package-state={exportBlocked ? 'blocked' : exportTrusted ? 'ready' : 'draft'}
            data-export-structural-state={exportTrustAxes.structural}
            data-export-verification-trust={exportTrustAxes.verificationTrust}
            data-export-action-state={exportTrustAxes.action}
            data-export-derived-state={exportTrustAxes.derived}
          >
            <div className="rb-pkg-decision-copy">
              <p className="ide-surface-block-label">Build &amp; Export</p>
              <h2>{surfaceStatusTitle}</h2>
              <p className="rb-pkg-trust-reason">{trustReason}</p>
            </div>
            <div
              className="rb-pkg-decision-state"
              data-testid="ide-export-trust-axes"
              aria-label="Export structure, verification trust, and download action"
            >
              <span>Derived state</span>
              <strong data-testid="ide-export-derived-state">
                {formatExportDerivedState(exportTrustAxes.derived)}
              </strong>
              <dl>
                <div data-testid="ide-export-structural-axis">
                  <dt>Structure</dt>
                  <dd>{exportTrustAxes.structural === 'downloadable' ? 'Downloadable' : 'Blocked'}</dd>
                </div>
                <div data-testid="ide-export-verification-axis">
                  <dt>Behavioral evidence</dt>
                  <dd data-testid="ide-export-simulation-evidence-tier">
                    {formatBehavioralEvidenceTier(behavioralEvidenceTier)}
                  </dd>
                </div>
                <div data-testid="ide-export-action-axis">
                  <dt>Action</dt>
                  <dd>{exportTrustAxes.action === 'downloaded' ? 'Downloaded' : 'Not downloaded'}</dd>
                </div>
              </dl>
              <small>{viewModel.artifacts.length} generated file{viewModel.artifacts.length === 1 ? '' : 's'}</small>
            </div>
            <div className="rb-pkg-primary" data-testid="ide-export-primary-actions">
              <IdeButton
                tone="secondary"
                onClick={() => setPackageValidation(
                  exportBlocked
                    ? {
                        status: 'blocked',
                        message: `${viewModel.errors.length || 1} package blocker${viewModel.errors.length === 1 ? '' : 's'} found.`,
                      }
                    : {
                        status: 'ready',
                        message: `${viewModel.artifacts.length} files structurally valid.`,
                      }
                )}
                testId="ide-export-validate-package"
              >
                Validate package
              </IdeButton>
              {onOpenDocument ? (
                <IdeButton tone="ghost" onClick={() => onOpenDocument({ kind: 'handoff' })} testId="ide-export-open-handoff">
                  Handoff overview
                </IdeButton>
              ) : null}
              {exportBlocked ? (
                <IdeButton
                  tone="primary"
                  onClick={blockedRecovery.action ?? (() => {})}
                  disabled={!blockedRecovery.action}
                  testId={blockedRecovery.testId}
                >
                  {blockedRecovery.label}
                </IdeButton>
              ) : handoffTruth.primaryCtaIntent === 'program-handoff' ? (
                <IdeButton
                  tone="primary"
                  onClick={() => void handleDownloadExport('project')}
                  disabled={downloadDisabled}
                  testId="ide-export-package-download-v1"
                >
                  {isRebuilding ? 'Building package…' : 'Download Package'}
                </IdeButton>
              ) : (
                <IdeButton
                  tone="primary"
                  onClick={handlePrimaryHandoff}
                  disabled={primaryHandoffDisabled}
                  testId="ide-export-package-build-v1"
                >
                  {primaryExportCtaLabel}
                </IdeButton>
              )}
              {isDraftExport && !primaryExportActionDownloadsProject ? (
                <IdeButton
                  tone="secondary"
                  onClick={() => void handleDownloadExport('project')}
                  disabled={downloadDisabled}
                  testId="ide-export-draft-download-v1"
                >
                  Download draft
                </IdeButton>
              ) : null}
              {!exportBlocked && handoffTruth.primaryCtaIntent === 'program-handoff' ? (
                <IdeButton
                  tone="ghost"
                  onClick={() => void handleDownloadExport('kit')}
                  disabled={downloadDisabled}
                  testId="ide-export-kit-download-v1"
                >
                  Download flat kit
                </IdeButton>
              ) : null}
              {packageValidation ? (
                <small className={`rb-pkg-validation is-${packageValidation.status}`} role="status" data-testid="ide-export-validation-result">
                  {packageValidation.message} Browser E0 only · Vivado external.
                </small>
              ) : null}
            </div>
          </section>

          {downloadError ? (
            <IdeCallout tone="error" title="Package could not be built" testId="ide-export-capsule-error">
              {downloadError}
            </IdeCallout>
          ) : null}
          {downloadDone ? (
            <IdeCallout
              tone="success"
              title={`${formatExportVerificationTrust(currentDownloadEvidence?.verificationTrust ?? 'unverified')} package downloaded`}
              testId="ide-export-download-success"
            >
              {formatExportTrustDetail(currentDownloadEvidence?.verificationTrust ?? 'unverified')} Package SHA-256:{' '}
              <code>{currentDownloadEvidence?.packageHash ?? currentDownloadEvidence?.bundleHash ?? 'legacy evidence unavailable'}</code>
            </IdeCallout>
          ) : null}

          {exportHistory.length > 0 ? <ExportHistoryPanel history={exportHistory} /> : null}

          <section
            className="rb-pkg-files"
            data-testid="ide-export-package-contents"
            aria-labelledby="ide-export-v3-files-title"
          >
            <header className="rb-pkg-section-header">
              <div>
                <p className="ide-surface-block-label">Package files</p>
              </div>
              <p>{readyArtifactCount} ready · {viewModel.artifacts.length} total</p>
            </header>
            {activeDocument?.kind === 'handoff' ? (
              <HandoffOverviewDocument
                project={project}
                projectName={(project.name ?? 'Project').trim()}
                boardLabel={(project.fpga?.board ?? 'basys3').replace(/^./, (c) => c.toUpperCase())}
                fpgaPart={project.fpga?.part ?? ''}
                topName={viewModel.topAuthority.designTop}
                viewModel={viewModel}
                lastRun={verifyLastRun}
                packageHash={determinismHash}
                stateTitle={surfaceStatusTitle}
                stateReason={trustReason}
                isStale={dirtySinceVerify}
                activeConstraintSetName={null}
                onOpenFiles={onOpenDocument ? () => onOpenDocument({ kind: 'package-artifact' }) : undefined}
              />
            ) : (
            <div className="rb-pkg-file-workspace" data-testid="ide-export-package-files">
              <nav
                className="rb-pkg-file-list"
                data-testid="ide-export-file-browser"
                aria-label="Generated package files"
              >
                <div className="rb-pkg-file-groups" data-testid="ide-export-artifact-tabs">
                  {artifactGroups.map((group) => (
                    <section
                      key={group.id}
                      className="rb-pkg-file-group"
                      data-testid={`ide-export-artifact-role-${group.id}`}
                      data-artifact-role={group.id}
                    >
                      <header>
                        <strong>{group.label}</strong>
                        <span>{group.artifacts.length}</span>
                      </header>
                      <div>
                        {group.artifacts.length > 0 ? group.artifacts.map((artifact) => {
                          const slug = artifact.path.toLowerCase().replace(/[^a-z0-9]+/g, '-');
                          const availability = formatArtifactAvailability(
                            artifact.status,
                            downloadReady,
                            exportTrusted
                          );
                          return (
                            <button
                              key={artifact.path}
                              type="button"
                              className={selectedArtifact?.path === artifact.path ? 'is-selected' : ''}
                              aria-pressed={selectedArtifact?.path === artifact.path}
                              onClick={() => setSelectedArtifactPath(artifact.path)}
                              data-testid={'ide-export-file-' + slug}
                            >
                              <span className="rb-pkg-file-icon" aria-hidden="true">
                                {formatArtifactGlyph(artifact.path)}
                              </span>
                              <span className="rb-pkg-file-name">{artifact.path}</span>
                              <strong
                                className="rb-pkg-file-availability"
                                aria-label={availability}
                                title={availability}
                              >
                                <span aria-hidden="true">{artifact.status === 'ready' ? '✓' : '·'}</span>
                                <span className="rb-pkg-file-availability-label">{availability}</span>
                              </strong>
                            </button>
                          );
                        }) : (
                          <p className="rb-pkg-file-role-empty">No file is generated for this role yet.</p>
                        )}
                      </div>
                    </section>
                  ))}
                </div>
              </nav>
              <article
                className="rb-pkg-preview"
                data-testid="ide-export-selected-preview-v1"
                aria-live="polite"
              >
                {selectedArtifact ? (
                  <>
                    <header>
                      <div>
                        <span>Selected file</span>
                        <strong data-testid="ide-export-preview-path">{selectedArtifact.path}</strong>
                        <p>{selectedArtifact.note}</p>
                      </div>
                      <div className="rb-pkg-preview-actions">
                        <IdeButton
                          tone="secondary"
                          onClick={() => void copyToClipboard(selectedArtifact.content, 'artifact')}
                          disabled={selectedArtifact.content.length === 0}
                          testId="ide-export-copy-current-file"
                        >
                          {copiedTarget === 'artifact' ? 'Copied' : 'Copy file'}
                        </IdeButton>
                        <IdeButton
                          tone="ghost"
                          onClick={() => handleDownloadArtifact(selectedArtifact)}
                          disabled={selectedArtifact.content.length === 0}
                          testId="ide-export-download-current-file"
                        >
                          Download file
                        </IdeButton>
                      </div>
                    </header>
                    <div data-testid="ide-export-artifact-preview">
                      <pre data-testid="ide-export-preview-code">
                        {(selectedArtifact.content || 'File content will appear when its owning stage is ready.')
                          .split('\n')
                          .map((line, index) => (
                            <span className="rb-pkg-code-line" key={`${selectedArtifact.path}-${index}`}>
                              <span className="rb-pkg-line-number" aria-hidden="true">{index + 1}</span>
                              <span>{line.length > 0 ? line : ' '}</span>
                            </span>
                          ))}
                      </pre>
                    </div>
                  </>
                ) : (
                  <p>No generated package files are available yet.</p>
                )}
              </article>
              <aside className="rb-pkg-handoff" data-testid="ide-export-handoff-inspector" aria-label="Vivado handoff inspector">
                <header><span>Handoff inspector</span><strong>{formatExportDerivedState(exportTrustAxes.derived)}</strong></header>
                <dl>
                  <div><dt>Evidence tier</dt><dd>{formatBehavioralEvidenceTier(behavioralEvidenceTier)}</dd></div>
                  <div><dt>Top module</dt><dd>{topModule}</dd></div>
                  <div><dt>Target board</dt><dd>{boardTargetLabel}</dd></div>
                  <div><dt>FPGA part</dt><dd>{vivadoPart}</dd></div>
                  <div><dt>Ownership</dt><dd>RedByte generated handoff</dd></div>
                  <div><dt>Problems</dt><dd>{packageProblemCount}</dd></div>
                </dl>
                <section>
                  <span>What to submit</span>
                  <p>Submit the roles requested by your instructor, commonly <code>top.vhd</code> and <code>top.xdc</code>.</p>
                </section>
                <section>
                  <span>Vivado next</span>
                  <ol><li>Download and unzip the package.</li><li>Run the included import Tcl or open the generated project.</li><li>Review synthesis and implementation warnings before bitstream generation.</li></ol>
                </section>
                <IdeButton tone="primary" onClick={() => void handleDownloadExport('project')} disabled={downloadDisabled} testId="ide-export-handoff-download">
                  {isRebuilding ? 'Building package…' : 'Download package'}
                </IdeButton>
              </aside>
            </div>
            )}
          </section>

          <section
            className="rb-pkg-upstream"
            data-testid="ide-export-upstream-readiness"
            aria-labelledby="ide-export-v3-upstream-title"
          >
            <header className="rb-pkg-section-header">
              <div>
                <p className="ide-surface-block-label">Upstream readiness</p>
              </div>
            </header>
            <div className="rb-pkg-upstream-rows">
              {upstreamReadinessRows.map((row) => (
                <article
                  key={row.id}
                  className={row.ready ? 'is-ready' : 'is-blocked'}
                  data-testid={'ide-export-upstream-' + row.id}
                  data-owner={row.owner}
                >
                  <span>{row.owner}</span>
                  <strong>{row.status}</strong>
                  <p>{row.detail}</p>
                </article>
              ))}
            </div>
          </section>

          <section
            className="rb-pkg-submission"
            data-testid="ide-export-submission-guidance"
            aria-labelledby="ide-export-v3-submission-title"
          >
            <header className="rb-pkg-section-header">
              <div>
                <p className="ide-surface-block-label">Vivado submission guide</p>
                <h3 id="ide-export-v3-submission-title">Choose files by requested role</h3>
              </div>
            </header>
            <div className="rb-pkg-submission-roles">
              {VIVADO_SUBMISSION_GUIDANCE.map((item) => (
                <article
                  key={item.role}
                  data-testid={`ide-export-submission-role-${item.role}`}
                  data-submission-role={item.role}
                >
                  <strong>{item.label}</strong>
                  <p>{item.guidance}</p>
                </article>
              ))}
            </div>
          </section>

          {guidedLabTask && guidedLabExportSummary ? (
            <section className="rb-pkg-lab" data-testid="ide-export-guided-full-adder-summary">
              <div>
                <p className="ide-surface-block-label">Lab submission</p>
                <h3>{guidedLabTask.title}</h3>
                <p>{guidedLabTask.submitGoal}</p>
              </div>
              <strong>{guidedLabExportSummary.packageStatus === 'ready' ? 'Package ready' : 'Package needs work'}</strong>
            </section>
          ) : null}

          <p className="rb-pkg-boundary" data-testid="ide-export-e0-boundary-summary">
            Browser E0 confirms package generation only. Vivado build, bitstream, programming, and physical board behavior remain external.
          </p>

          <section className="rb-pkg-technical-entry" aria-label="Technical package evidence">
            <div>
              <p className="ide-surface-block-label">Technical evidence</p>
            </div>
            <IdeButton
              tone="secondary"
              onClick={() => setTechnicalEvidenceOpen(true)}
              testId="ide-export-open-technical-evidence"
            >
              Open technical evidence
            </IdeButton>
          </section>

          {technicalEvidenceOpen ? (
            <div className="rb-pkg-dialog-backdrop">
              <section
                className="rb-pkg-dialog"
                role="dialog"
                aria-modal="true"
                aria-labelledby="ide-export-v3-dialog-title"
                data-testid="ide-export-technical-dialog"
              >
                <header>
                  <div>
                    <p className="ide-surface-block-label">Technical evidence</p>
                    <h2 id="ide-export-v3-dialog-title">Package diagnostics and provenance</h2>
                  </div>
                  <IdeButton
                    tone="secondary"
                    onClick={() => setTechnicalEvidenceOpen(false)}
                    testId="ide-export-close-technical-evidence"
                  >
                    Close
                  </IdeButton>
                </header>

                <section>
                  <h3>Readiness gates</h3>
                  <div className="rb-pkg-gate-list" data-testid="ide-export-gate-stack">
                    {gateRows.map((gate) => (
                      <article key={gate.id} data-testid={'ide-export-gate-' + gate.id}>
                        <strong>{gate.label}</strong>
                        <span>{gate.tone === 'ok' ? 'Ready' : gate.tone === 'warn' ? 'Review' : 'Needs fix'}</span>
                        <p>{gate.detail}</p>
                        {gate.onAction ? (
                          <IdeButton tone="ghost" onClick={gate.onAction} testId={'ide-export-gate-action-' + gate.id}>
                            {gate.actionLabel}
                          </IdeButton>
                        ) : null}
                      </article>
                    ))}
                  </div>
                </section>

                <section>
                  <h3>Deterministic checks</h3>
                  <ul data-testid="ide-export-deterministic-checks">
                    {deterministicChecks.map((check) => (
                      <li key={check.id}>
                        <strong>{check.pass ? 'Pass' : 'Review'} · {check.label}</strong>
                        <span>{check.tooltip}</span>
                      </li>
                    ))}
                  </ul>
                </section>

                <section>
                  <h3>Diagnostics</h3>
                  <div className="rb-pkg-diagnostics" data-testid="ide-export-blockers-list">
                    {visibleDiagnosticsList.length > 0 ? visibleDiagnosticsList.map((entry) => (
                      <article key={entry.id} data-testid={'ide-export-diagnostic-' + entry.id}>
                        <div><code>{entry.code}</code><strong>{entry.title}</strong></div>
                        <p>{entry.message}</p>
                        <small>Owner: {formatExportDiagnosticOwner(entry.owner)}</small>
                      </article>
                    )) : <p>No export diagnostics.</p>}
                  </div>
                </section>

                <section className="rb-pkg-provenance">
                  <h3>Build identity</h3>
                  <dl>
                    <div><dt>RedByte version</dt><dd>{redbyteVersion}</dd></div>
                    <div><dt>Commit</dt><dd>{redbyteCommit}</dd></div>
                    <div><dt>Export hash</dt><dd>{viewModel.exportHash ?? 'pending'}</dd></div>
                    <div><dt>Simulation hash</dt><dd>{verifyResult?.hash ?? 'pending'}</dd></div>
                  </dl>
                  {currentDownloadEvidence ? (
                    <section data-testid="ide-export-download-evidence">
                      <h3>Browser-local download evidence</h3>
                      <dl>
                        <div>
                          <dt>Package SHA-256</dt>
                          <dd>{currentDownloadEvidence.packageHash ?? currentDownloadEvidence.bundleHash ?? 'legacy evidence unavailable'}</dd>
                        </div>
                        <div>
                          <dt>Downloaded at</dt>
                          <dd>{currentDownloadEvidence.downloadedAtIso ?? currentDownloadEvidence.ranAtIso ?? 'unknown'}</dd>
                        </div>
                        <div>
                          <dt>Trust at download</dt>
                          <dd>{formatExportVerificationTrust(currentDownloadEvidence.verificationTrust ?? 'unverified')}</dd>
                        </div>
                        <div>
                          <dt>Project source</dt>
                          <dd>{formatExportSourceEvidence(currentDownloadEvidence.sourceHashes?.project, currentDownloadEvidence.sourceCurrentness?.project)}</dd>
                        </div>
                        <div>
                          <dt>Export source</dt>
                          <dd>{formatExportSourceEvidence(currentDownloadEvidence.sourceHashes?.export, currentDownloadEvidence.sourceCurrentness?.export)}</dd>
                        </div>
                        <div>
                          <dt>Mapping source</dt>
                          <dd>{formatExportSourceEvidence(undefined, currentDownloadEvidence.sourceCurrentness?.mapping)}</dd>
                        </div>
                        <div>
                          <dt>Simulation source</dt>
                          <dd>{formatExportSourceEvidence(currentDownloadEvidence.sourceHashes?.verify, currentDownloadEvidence.sourceCurrentness?.verify)}</dd>
                        </div>
                      </dl>
                    </section>
                  ) : null}
                  <IdeButton
                    tone="secondary"
                    onClick={() => void copyToClipboard(quickDebugReport, 'report')}
                    testId="ide-export-copy-report"
                  >
                    {copiedTarget === 'report' ? 'Copied debug report' : 'Copy debug report'}
                  </IdeButton>
                </section>
              </section>
            </div>
          ) : null}
        </section>
      </IdePanel>
    </IdeSurfaceLayout>
  );
};

function pickArtifactStatus(
  map: Map<string, ExportArtifactView>,
  filename: string
): 'ready' | 'blocked' | 'pending' | 'missing' {
  const hit = map.get(filename.toLowerCase());
  if (!hit) return 'missing';
  return hit.status;
}

function formatArtifactAvailability(
  status: ExportArtifactView['status'],
  downloadReady: boolean,
  hasTrustedCurrentReceipt: boolean
): string {
  if (status === 'blocked') return 'Blocked';
  if (status !== 'ready') return 'Pending';
  if (!downloadReady) return 'Generated';
  return hasTrustedCurrentReceipt ? 'Ready' : 'Downloadable';
}

function formatArtifactGlyph(path: string): string {
  const extension = path.split('.').pop()?.toLowerCase();
  if (extension === 'vhd') return 'VH';
  if (extension === 'xdc') return 'XC';
  if (extension === 'tcl') return 'TC';
  if (extension === 'json') return '{}';
  if (extension === 'md') return 'MD';
  return 'TX';
}

function buildArtifactGroups(artifacts: ExportArtifactView[]): ExportArtifactGroup[] {
  const groups: ExportArtifactGroup[] = [
    {
      id: 'source',
      label: 'Source',
      description:
        'Synthesizable VHDL design sources, including the authoritative top-level entity.',
      artifacts: [],
    },
    {
      id: 'constraints',
      label: 'Constraints',
      description:
        'Board pin and timing constraints aligned to the current Board & Constraints authority.',
      artifacts: [],
    },
    {
      id: 'simulation',
      label: 'Simulation',
      description:
        'Simulation-only testbench sources. These never enter the Vivado design source set.',
      artifacts: [],
    },
    {
      id: 'project',
      label: 'Project',
      description:
        'Vivado setup scripts and RedByte project metadata for a reproducible project handoff.',
      artifacts: [],
    },
    {
      id: 'readme',
      label: 'README',
      description:
        'Setup, provenance, and board bring-up guidance for the person receiving the package.',
      artifacts: [],
    },
  ];
  const index = new Map(groups.map((group) => [group.id, group]));
  for (const artifact of artifacts) {
    const groupId = classifyArtifactGroup(artifact);
    index.get(groupId)?.artifacts.push(artifact);
  }
  return groups;
}

function classifyArtifactGroup(artifact: ExportArtifactView): ExportArtifactGroupId {
  if (artifact.kind === 'xdc') return 'constraints';
  if (artifact.category === 'simulation-source' || artifact.kind === 'tb') return 'simulation';
  if (artifact.category === 'design-source' || artifact.kind === 'vhd') return 'source';
  if (artifact.kind === 'readme' || artifact.kind === 'md') return 'readme';
  return 'project';
}

function deriveExportVerifySourceState(
  verifyState: ReturnType<typeof deriveProjectVerifyState>,
  comparePassIncomplete: boolean
): ProjectHealthExportSourceState {
  if (comparePassIncomplete) return 'incomplete';
  switch (verifyState) {
    case 'assertions-match':
      return 'current';
    case 'assertions-differ':
    case 'verify-error':
      return 'failed';
    case 'stale':
      return 'stale';
    case 'trace':
      return 'trace-only';
    case 'not-run':
      return 'missing';
  }
}

function formatExportVerificationTrust(
  trust: NonNullable<ProjectHealthExportResult['verificationTrust']>
): string {
  if (trust === 'trusted') return 'Trusted';
  if (trust === 'draft') return 'Draft';
  return 'Unverified';
}

function formatExportTrustDetail(
  trust: NonNullable<ProjectHealthExportResult['verificationTrust']>
): string {
  if (trust === 'trusted') {
    return 'Current Compare evidence matched when this package was downloaded.';
  }
  if (trust === 'draft') {
    return 'The package is structurally valid, but its Compare evidence was stale or did not match.';
  }
  return 'The package is structurally valid, but no current assertion-backed Compare evidence exists.';
}

function formatExportSourceEvidence(
  hash: string | undefined,
  currentness: ProjectHealthExportSourceState | undefined
): string {
  const hashLabel = hash && hash.length > 0 ? hash : 'hash not recorded';
  return `${hashLabel} (${currentness ?? 'currentness not recorded'})`;
}

function resolveExportLayoutMode(width?: number): ExportLayoutMode {
  const nextWidth =
    typeof width === 'number'
      ? width
      : typeof window !== 'undefined'
        ? window.innerWidth
        : 1440;
  if (nextWidth >= 1440) return 'wide';
  if (nextWidth >= 1280) return 'standard';
  return 'compact';
}

function createPinOverrideMap(
  rows: ExportPinTableRow[]
): Record<string, string> {
  const overrides: Record<string, string> = {};
  for (const row of rows) {
    overrides[toPortKey(row.port)] = normalizePinForExportDisplay(row.pin ?? '');
  }
  return overrides;
}

function collectMappedProjectPortKeys(project: RBProject): Set<string> {
  const keys = new Set<string>();
  const liveIoNodeLabels = buildLiveIoNodeLabelIndex(project);
  for (const entry of project.ioMapping?.inputs ?? []) {
    keys.add(toMappingEntryKey(entry, liveIoNodeLabels));
  }
  for (const entry of project.ioMapping?.outputs ?? []) {
    keys.add(toMappingEntryKey(entry, liveIoNodeLabels));
  }
  return keys;
}

function applyPinOverridesToProject(
  project: RBProject,
  overrides: Record<string, string>
): RBProject {
  if (!project.ioMapping) return project;
  const liveIoNodeLabels = buildLiveIoNodeLabelIndex(project);

  const applyEntries = (entries: NonNullable<RBProject['ioMapping']>['inputs']) =>
    entries.map((entry) => {
      const override = (overrides[toMappingEntryKey(entry, liveIoNodeLabels)] ?? entry.pin ?? '').trim();
      return {
        ...entry,
        pin: override.length > 0 ? override : undefined,
      };
    });

  return {
    ...project,
    ioMapping: {
      inputs: applyEntries(project.ioMapping.inputs ?? []),
      outputs: applyEntries(project.ioMapping.outputs ?? []),
    },
  };
}

function buildEvidenceDiagnostics(
  verifyResult: ProjectHealthVerifyResult | undefined,
  dirtySinceVerify: boolean,
  vectorsAreAutoGenerated = false,
  verifyQualification?: RuntimeVerifyRun['qualification']
): ExportDiagnosticView[] {
  const diagnostics: ExportDiagnosticView[] = [];
  const verifyState = deriveProjectVerifyState({
    lastVerify: verifyResult,
    dirtySinceVerify,
  });

  if (!verifyResult) {
    diagnostics.push(createEvidenceDiagnostic({
      code: 'RBEV1000',
      message: 'No comparison run found. Export files are still available, but no expected-output evidence has been recorded yet.',
      fix: 'Open Simulate when you want to compare expected outputs against the live design before relying on the package as final hardware evidence.',
      severity: 'warning',
    }));
    return diagnostics;
  }

  if (dirtySinceVerify) {
    diagnostics.push(createEvidenceDiagnostic({
      code: 'RBEV1002',
      message: 'Design, testbench, or mapping changed since the last simulation run. Export files remain available, but that evidence is stale for the current handoff.',
      fix: 'Open Simulate and rerun the scenario or Compare when you want current evidence for this export.',
      severity: 'warning',
    }));
  } else if (verifyState === 'trace') {
    diagnostics.push(createEvidenceDiagnostic({
      code: 'RBEV1004',
      message: 'Latest simulation activity recorded a trace-only run. Export files are still available, but expected-output comparison has not been confirmed yet.',
      fix: 'Open Simulate and run Compare checks when you want current evidence for trusted export.',
      severity: 'warning',
    }));
  }

  if (!dirtySinceVerify && verifyResult.status !== 'pass') {
    const isStarterFail = vectorsAreAutoGenerated;
    diagnostics.push(createEvidenceDiagnostic({
      code: 'RBEV1001',
      message: isStarterFail
        ? 'Ran with auto-generated starter vectors - scenario not yet authored. Export is available, but the comparison evidence is still starter-grade.'
        : typeof verifyResult.failingTick === 'number'
          ? `Latest comparison run differed at tick ${verifyResult.failingTick}. Export files remain available.`
          : 'Latest comparison run differed from observed outputs. Export files remain available.',
      fix: isStarterFail
        ? 'Author a real test scenario in Simulate (not starter vectors) before treating this export as evidence-grade.'
        : 'Open Simulate, inspect the first difference, then rerun Compare when you want refreshed evidence.',
      severity: 'warning',
    }));
  }

  if (verifyQualification === 'incomplete-mapping') {
    diagnostics.push(createEvidenceDiagnostic({
      code: 'RBEV1003',
      message:
        'Latest comparison run matched the logic, but required output mapping was incomplete. Export files are available, but hardware interpretation is still incomplete.',
      fix:
        'Complete the live output pin mapping, then rerun Compare when you want refreshed evidence against the current design.',
      severity: 'warning',
    }));
  }

  return diagnostics;
}

function createEvidenceDiagnostic(input: {
  code: string;
  message: string;
  fix: string;
  severity?: ExportDiagnosticSeverity;
}): ExportDiagnosticView {
  const severity = input.severity ?? 'warning';
  const canonical = createIdeDiagnostic({
    severity: severity === 'error' ? 'error' : 'warn',
    code: input.code,
    title: severity === 'error' ? 'Evidence gate blocker' : 'Evidence advisory',
    message: input.message,
    hint: [input.fix],
    owner: {
      kind: 'file',
      filePath: 'verify-report.json',
    },
    origin: 'export',
    stage: 'export',
    location: {
      filePath: 'verify-report.json',
    },
    actions: [
      {
        kind: 'open-mode',
        label: 'Open Simulate',
        payload: {
          mode: 'verify',
          filePath: 'verify-report.json',
        },
      },
    ],
  });

  return {
    id: canonical.id,
    code: canonical.code,
    title: canonical.title,
    message: canonical.message,
    hint: canonical.hint,
    fix: input.fix,
    severity,
    owner: canonical.owner,
    actions: canonical.actions,
    canonical,
  };
}

function toPortKey(value: string): string {
  return value.trim().toLowerCase();
}

function toMappingEntryKey(entry: {
  id: string;
  nodeId: string;
  port: string;
  label?: string;
}, liveIoNodeLabels?: ReadonlyMap<string, string>): string {
  return toPortKey(resolveMappingPortName(entry, liveIoNodeLabels));
}

function statusTone(status: ExportPinStatus): 'ok' | 'error' | 'warn' {
  if (status === 'mapped') return 'ok';
  if (status === 'missing') return 'error';
  return 'warn';
}

function resolveTopEntity(project: RBProject): string {
  const top = (project.hdl?.top ?? project.fpga?.top ?? '')
    .trim()
    .replace(/[^A-Za-z0-9_]+/g, '_');
  return top.length > 0 ? top : 'top';
}

function resolveRowStatus(baseStatus: ExportPinStatus, pinValue: string): ExportPinStatus {
  if (baseStatus === 'unused') return 'unused';
  return pinValue.trim().length > 0 ? 'mapped' : 'missing';
}

function getPinConfidence(
  suggestedPin: string | undefined,
  pinValue: string
): 'exact' | 'likely' | 'unknown' {
  if (!suggestedPin) return 'unknown';
  const suggestedPackagePin = resolveBasys3PackagePin(suggestedPin) ?? suggestedPin.trim().toUpperCase();
  const actualPackagePin = resolveBasys3PackagePin(pinValue) ?? pinValue.trim().toUpperCase();
  if (actualPackagePin === suggestedPackagePin) return 'exact';
  return 'likely';
}

function normalizePinForExportDisplay(pin: string): string {
  const trimmed = pin.trim();
  if (trimmed.length === 0) return '';
  return resolveBasys3PackagePin(trimmed) ?? trimmed;
}

function formatLabelFirstPinBinding(pin: string): string {
  const trimmed = pin.trim();
  if (trimmed.length === 0) return '';
  const packagePin = resolveBasys3PackagePin(trimmed);
  if (!packagePin) return trimmed.toUpperCase();
  const alias = resolveBasys3BoardAlias(trimmed);
  return alias ? `${alias} (pin ${packagePin})` : packagePin;
}

function buildInvalidPinSet(
  overrides: Record<string, string>,
  editablePortKeys: Set<string>
): Set<string> {
  const invalid = new Set<string>();
  for (const [portKey, pin] of Object.entries(overrides)) {
    if (!editablePortKeys.has(portKey)) continue;
    const trimmed = pin.trim();
    if (trimmed.length > 0 && !BASYS3_VALID_PINS.has(trimmed)) {
      invalid.add(portKey);
    }
  }
  return invalid;
}

// ─── Phase 2: Syntax Highlighting ──────────────────────────────────────────

const VHDL_KEYWORDS = new Set([
  'library','use','entity','architecture','is','begin','end','port','map',
  'signal','component','generic','process','if','then','else','elsif','case',
  'when','others','not','and','or','nor','nand','xor','xnor',
  'std_logic','std_logic_vector','downto','to','in','out','inout','buffer',
  'all','of','type','subtype','constant','variable','integer','boolean',
  'true','false',
]);

const VERILOG_KEYWORDS = new Set([
  'module','endmodule','input','output','inout','wire','reg','assign',
  'always','begin','end','if','else','case','endcase','posedge','negedge',
  'parameter','localparam','and','or','not','nand','nor','xor','xnor',
]);

const XDC_PROPS = new Set([
  'set_property','get_ports','create_clock','set_input_delay','set_output_delay',
]);

const TCL_COMMANDS = new Set([
  'proc','set','if','else','foreach','while','return','puts','source','package',
]);

function escapeHtml(text: string): string {
  return text
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;');
}

function highlightKeywords(code: string, keywords: Set<string>): string {
  return code.replace(/\b([A-Za-z_][A-Za-z0-9_]*)\b/g, (match) => {
    return keywords.has(match.toLowerCase()) || keywords.has(match)
      ? `<span class="hl-kw">${match}</span>`
      : match;
  });
}

function wrapComment(text: string): string {
  return `<span class="hl-comment">${text}</span>`;
}

function syntaxHighlight(code: string, kind: string): string {
  const lines = code.split('\n');

  if (kind === 'vhd' || kind === 'tb') {
    // VHDL / testbench: line comments start with --
    return lines.map((line) => {
      const escaped = escapeHtml(line);
      const commentIdx = escaped.indexOf('--');
      if (commentIdx === -1) {
        return highlightKeywords(escaped, VHDL_KEYWORDS);
      }
      const codePart = escaped.slice(0, commentIdx);
      const commentPart = escaped.slice(commentIdx);
      return highlightKeywords(codePart, VHDL_KEYWORDS) + wrapComment(commentPart);
    }).join('\n');
  }

  if (kind === 'verilog') {
    // Verilog: // line comments and /* */ block comments (single-line only)
    return lines.map((line) => {
      const escaped = escapeHtml(line);
      const lineCommentIdx = escaped.indexOf('//');
      if (lineCommentIdx !== -1) {
        const codePart = escaped.slice(0, lineCommentIdx);
        const commentPart = escaped.slice(lineCommentIdx);
        return highlightKeywords(codePart, VERILOG_KEYWORDS) + wrapComment(commentPart);
      }
      const blockStart = escaped.indexOf('/*');
      if (blockStart !== -1) {
        const blockEnd = escaped.indexOf('*/', blockStart + 2);
        if (blockEnd !== -1) {
          const before = escaped.slice(0, blockStart);
          const comment = escaped.slice(blockStart, blockEnd + 2);
          const after = escaped.slice(blockEnd + 2);
          return (
            highlightKeywords(before, VERILOG_KEYWORDS)
            + wrapComment(comment)
            + highlightKeywords(after, VERILOG_KEYWORDS)
          );
        }
      }
      return highlightKeywords(escaped, VERILOG_KEYWORDS);
    }).join('\n');
  }

  if (kind === 'xdc') {
    // XDC: # line comments; property-name keywords
    return lines.map((line) => {
      const escaped = escapeHtml(line);
      const commentIdx = escaped.indexOf('#');
      if (commentIdx !== -1) {
        const codePart = escaped.slice(0, commentIdx);
        const commentPart = escaped.slice(commentIdx);
        return highlightKeywords(codePart, XDC_PROPS) + wrapComment(commentPart);
      }
      return highlightKeywords(escaped, XDC_PROPS);
    }).join('\n');
  }

  if (kind === 'tcl') {
    // TCL: # line comments; command keywords
    return lines.map((line) => {
      const escaped = escapeHtml(line);
      const commentIdx = escaped.indexOf('#');
      if (commentIdx !== -1) {
        const codePart = escaped.slice(0, commentIdx);
        const commentPart = escaped.slice(commentIdx);
        return highlightKeywords(codePart, TCL_COMMANDS) + wrapComment(commentPart);
      }
      return highlightKeywords(escaped, TCL_COMMANDS);
    }).join('\n');
  }

  // Passthrough for unrecognized kinds (readme, md, json, etc.)
  return escapeHtml(code);
}

function downloadZipBytes(bytes: Uint8Array, fileName: string): void {
  if (typeof window === 'undefined') return;
  const zipBuffer = Uint8Array.from(bytes).buffer;
  const blob = new Blob([zipBuffer], { type: 'application/zip' });
  const url = URL.createObjectURL(blob);
  const link = document.createElement('a');
  link.href = url;
  link.download = fileName;
  document.body.appendChild(link);
  link.click();
  document.body.removeChild(link);
  window.setTimeout(() => URL.revokeObjectURL(url), 0);
}

async function buildVivadoKitZip(artifacts: ExportArtifactView[]): Promise<Uint8Array> {
  return buildDeterministicZip(
    artifacts
      .filter((artifact) => artifact.content.trim().length > 0)
      .map((artifact) => ({
        name: artifact.path,
        text: artifact.content,
      }))
  );
}

// ─── Phase 2: Pin Validation ────────────────────────────────────────────────

const BASYS3_VALID_PINS = new Set([
  // Switches
  'V17','V16','W16','W17','W15','V15','W14','W13','V2','T3','T2','R3','W2','U1','T1','R2',
  // LEDs
  'U16','E19','U19','V19','W18','U15','U14','V14','V13','V3','W3','U3','P3','N3','P1','L1',
  // Buttons
  'W19','T17','T18','U17','U18',
  // Clock
  'W5',
  // PMOD (JA, JB, JC, JXADC)
  'J1','L2','J2','G2','H1','K2','H2','G3',
  'A14','A16','B15','B16','A15','A17','C15','C16',
  'K17','M18','N17','P18','L17','M19','P17','R18',
  'J3','L3','M2','N2','K3','M3','M1','N1',
  // VGA
  'G19','H19','J19','N19','N18','L18','K18','J18','J17','H17','G17','D17',
  // USB-UART
  'B18','A18',
  // QSPI
  'D18','D19','G18','F18','K19',
  // SDSPI
  'E2','A1','B1','C1',
]);
