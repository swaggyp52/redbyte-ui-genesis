export type { NodeMeta, SequentialAnalysis } from '@redbyte/rb-logic-core';
export {
  analyzeSequentialLogic,
  getClockPortName,
  getNodeMeta,
  getResetPortName,
  isSequentialNodeType,
} from '@redbyte/rb-logic-core';
