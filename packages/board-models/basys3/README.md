# Basys 3 board model

Canonical pinmap: `pinmap.vivado.xdc`

## UART (USB-RS232)

- `RsRx` (B18) RX
- `RsTx` (A18) TX
- 115200 8N1

These nets are used by RedByte fixtures and bridge streaming wrappers.
