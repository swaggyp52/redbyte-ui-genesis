# Spartan-3E Starter Kit board model

Canonical pinmap: `pinmap.ise.ucf`

## UART (RS-232 DCE)

- `rs232_dce_rxd` (R7) RX
- `rs232_dce_txd` (M14) TX
- 115200 8N1

These nets are used by RedByte fixtures and bridge streaming wrappers.
